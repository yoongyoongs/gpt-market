# V3 第五轮整改落实记录（2026-09-04）

> 依据《gpt-market_V3_第五轮代码复验报告_终验指导修订版_2026-09-04.md》§47 顺序、
> §56-§73 强制验收规范执行。范围冻结：只做 R5-01～R5-08（§56.1），不接 GLM、
> 不做强势启动、不做 AI Access、不重做 Dashboard、不扩策略、不动 V1/V2。
>
> 状态口径（§58）：NOT_STARTED / IMPLEMENTED / TESTED / READY_FOR_EXTERNAL_REVIEW /
> BLOCKED。不使用 CLOSED / 100%——CLOSED 只能由外部复验确认。

## 1. Dependency Map（§57，改码前冻结）

横向链路：

```text
ProviderManager(eastmoney→tencent, Freshness: data_timestamp/server_timestamp/stale)
  ↓
IntradayMarketDataService.map_quote_snapshot / get_intraday_bars（V3 快照/BarSeries + provenance）
  ↓                                   ↓
IntradayFastLaneService               ReadEntryDecisionContext / ReadPositionContext
（quote 批量→Overlay→Scanner→          （quote+bars+facts→readiness）
  ActivePool→Deep→Attention）           ↓
  ↓                                   AttentionEngine.evaluate_entry_plan_levels
IntradayTriggerLoop（plan→quote→engine，Fast Lane 同循环）
  ↓
HTTP /api/v3 + MCP v3_tools → ChatGPT
```

| 项 | 上游输入 | 当前错误 | 修改位置 | 下游影响 |
|----|---------|---------|---------|---------|
| R5-01 | Freshness 三元组（data_timestamp=事件时点 / server_timestamp=系统知道时点） | `map_quote_snapshot` 以 request_start=T0 对比 known_at → FUTURE_KNOWN_AT 误判新鲜行情；bars 顶层 known_at 在请求前取；per-period known_at 冒用 data_timestamp；Entry Context 顶层 known_at 在 fetch 前取 | `intraday_market_data.py`（Future Guard 重做+bars known_at）、`read_entry_decision_context.py`（聚合后 as_of/known_at=max(components)）、`app/api/v3.py`（`_require_current_as_of` 保留为 T0 起点） | Fast Lane 不再批量 stale；Entry readiness 正常；engine Gate 不误触发 DATA_QUALITY_DEGRADED；Position stop_hit 正常 |
| R5-02 | EntryPlanModel+DecisionModel+WatchlistModel+Portfolio | `active_price_trigger_plans` 取全部 decision 最新 plan → 历史/失效计划仍被监控 | `decision_repositories.py`（Repository 层冻结 Active Plan 定义） | Resident loop 只监控有效计划；历史 STOP_HIT/TARGET_HIT 消失 |
| R5-03 | ActivePoolEntry（含 sources 标签） | `report["deep"]=_deep_summaries(candidates)` 只喂 Scanner 候选 | `intraday_fast_lane.py`（Deep 输入改 merged pool，优先级 Portfolio>Watchlist>EOD>Intraday，deep_limit 裁剪） | Scanner 为空时 Portfolio/Watchlist/EOD 仍获 5m/15m/60m 深度刷新 |
| R5-04 | `get_all_a_shares()→(total,quotes)`；_load_state 四源 | 丢 total 无覆盖率；四源同 try 一损俱损 | `intraday_fast_lane.py`（expected/actual/missing/coverage + PARTIAL/UNAVAILABLE gate；四源独立 status/error/count） | loop heartbeat last_fast_lane_status 如实；MCP scan 不再把部分市场说成全市场 |
| R5-05 | RawOpportunityModel、WatchlistModel 现态、Feature 全量 | EOD 用 recall 前 200；Watchlist 用最近 500 变更事件；feature overlay 默认 2000 只无覆盖率 | `intraday_fast_lane.py`（EOD→read_raw；Watchlist→read_watchlist 现态过滤；feature_limit 6000 + feature_expected/actual/coverage） | Active Pool 来源符合 Source of Truth；长期 WATCHING 票不丢 |
| R5-06 | uow.evidence.retrieve_view（subject_id=`market:code`）；recalls repo | evidence 写死 NOT_AVAILABLE(NO_EVIDENCE_READ_API)；recall/raw 前 200 客户端过滤=漏检 | `read_entry_decision_context.py`（真实 Evidence 聚合）；`repositories.py`+`protocols.py`（latest_recall_for_security / latest_raw_opportunity_for_security） | Decision Context 含真实公告/催化/利空证据；Recall/Raw 精确按 security |
| R5-07 | loop、scheduler、OperationalHealthEventModel | 单一 300s cadence 同时跑止损与全市场；heartbeat 仅 worker 内存 | `intraday_loop.py`（due-time 拆分：trigger 45s / fastlane 90s / evidence 600s，env 可配）；heartbeat 节流写 operational_health_events；`read_worker_heartbeat.py` + GET /operations/worker-heartbeat 读表 | API/Dashboard 可见 worker degraded/last_error/consecutive_errors |
| R5-08 | AttentionEngine.record_new_evidence（无生产调用方）；typed plan trigger/cancel_condition | 盘中 NEW_EVIDENCE 链无调用方；Resident 只评 stop/target，ENTRY_TRIGGER_MET/CANCEL/STRUCTURE 不产生 | `attention_engine.py`（evaluate_entry_trigger_cancel，同 R4-02 Gate）；`intraday_event_poll.py`（新建盘中事件轮询）；`intraday_loop.py`（传完整 plan payload；evidence lane→record_new_evidence；结构翻转→STRUCTURE_CHANGED） | 盘中事件链闭环；安全边界不变（EVENT≠TRADE） |

## 2. 逐项状态与 Evidence Pack（§69）

测试命令统一为：`python -m pytest tests/v3/<file> -q --no-header -p no:warnings`。
全量回归基线：**tests/v3 → 444 passed, 90 skipped**（skip 均为 V3_TEST_DATABASE_URL
等门控，非失败改 skip）。

### R5-01 CURRENT Snapshot 时间语义（§59）

| 项 | 内容 |
|----|------|
| 状态 | TESTED |
| 提交 | `50586f1` |
| 修改 | `intraday_market_data.py`（Future Guard：对比基准改 known_at 自身三元组，不再用 request_start=T0；bars 顶层 known_at 取请求后时刻）、`read_entry_decision_context.py`（顶层 as_of/known_at = 各组件 max 聚合）、`domain/intraday.py`、`app/api/v3.py` |
| 正例 | quote data_timestamp=09:59:59、server_timestamp=10:00:01、known_at=10:00:02、请求 10:00:00 → 新鲜判定通过（旧逻辑 T0>known_at 误报 FUTURE_KNOWN_AT） |
| 负例 | known_at 晚于请求时刻的未来数据 → 仍报 FUTURE_KNOWN_AT；stale 三元组 → UNTRUSTED |
| 回归 | test_intraday_market_data 13 passed；test_entry_decision_context 22 passed 1 skipped；test_intraday_fast_lane 14 passed |
| 备注 | `_require_current_as_of` 保留为 T0 起点（§59.1 裁决） |

### R5-02 Active EntryPlan 定义与查询（§60）

| 项 | 内容 |
|----|------|
| 状态 | TESTED |
| 提交 | `d38e4aa` |
| 修改 | `infrastructure/db/decision_repositories.py`（Repository 层冻结 Active Plan 定义：仅有效 Decision 的最新 plan 进监控）、`tests/v3/test_active_entry_plan_filter.py`（新建） |
| 正例 | 同 decision 多版 plan → 只监控最新一版；历史/失效 decision 的 plan 不再触发 STOP_HIT/TARGET_HIT |
| 负例 | 无 Active Plan → 零评估零事件 |
| 回归 | test_active_entry_plan_filter 6 passed |

### R5-03 Deep 基于 merged Active Pool（§61）

| 项 | 内容 |
|----|------|
| 状态 | TESTED |
| 提交 | `7bcadd8` |
| 修改 | `intraday_fast_lane.py`（Deep 输入改 merged Active Pool，来源优先级 Portfolio>Watchlist>EOD>Intraday，deep_limit 裁剪） |
| 正例 | Scanner 候选为空、Portfolio/Watchlist 有券 → Deep 仍按池刷新 5m/15m/60m |
| 负例 | 池为空 → Deep 零调用 |
| 回归 | test_intraday_fast_lane 14 passed |

### R5-04 全市场 Coverage Gate + 来源失败隔离（§62）

| 项 | 内容 |
|----|------|
| 状态 | TESTED |
| 提交 | `3964979` |
| 修改 | `intraday_fast_lane.py`（expected/actual/missing/coverage + PARTIAL/UNAVAILABLE gate；quote/eod/watchlist/portfolio 四源独立 status/error/count） |
| 正例 | 全市场 5432 只、实际 5432 → coverage=1.0、AVAILABLE |
| 负例 | 部分失败 → coverage<1.0、PARTIAL/UNAVAILABLE_FOR_FULL_MARKET_SCAN（不再谎报全市场）；单源异常不影响其余源 |
| 回归 | test_intraday_fast_lane 14 passed |

### R5-05 EOD Raw Candidate + 现态 Watchlist + Feature Coverage（§63）

| 项 | 内容 |
|----|------|
| 状态 | TESTED |
| 提交 | `765721c` |
| 修改 | `intraday_fast_lane.py`（EOD 候选改用 read_raw 真数据源；Watchlist 改 read_watchlist 现态过滤替代"最近 500 变更事件"；feature overlay 限额 6000 + feature_expected/actual/coverage）、`decision_repositories.py`、`scripts/v3_scheduler.py` |
| 正例 | 长期 WATCHING 票（无近期变更事件）→ 仍进 Active Pool；feature overlay 全市场 → coverage 如实 |
| 负例 | 现态非 WATCHING/WAIT_ENTRY/ACTION_READY → 不进池 |
| 回归 | test_intraday_fast_lane 14 passed |

### R5-06 Evidence 真实聚合 + Security-specific Recall/Raw（§64）

| 项 | 内容 |
|----|------|
| 状态 | TESTED |
| 提交 | `3dc37d9` |
| 修改 | `read_entry_decision_context.py`（evidence 真实调用 `uow.evidence.retrieve_view`，NO_EVIDENCE_READ_API 占位移除；known_at 纳入顶层聚合）；`repositories.py` + `protocols.py`（`latest_recall_for_security` / `latest_raw_opportunity_for_security`，SQL 端按 market/code 过滤） |
| 正例 | 有 Published run 且命中 → AVAILABLE {count, items}；evidence known_at 晚于其余组件 → 顶层 as_of/known_at 被抬升、stale 重算 |
| 负例 | 无 run → NO_PUBLISHED_RECALL_RUN；run 在但该券无记录 → NOT_IN_LATEST_RECALL_RESULTS / NOT_IN_LATEST_RAW_OPPORTUNITY；evidence 异常 → SOURCE_FAILED 隔离不阻断 |
| 回归 | test_entry_decision_context 22 passed 1 skipped；test_recall_postgres 新增 SQL 断言（PG 门控，本机未跑） |

### R5-07 拆分 Cadence + Heartbeat 跨进程（§65）

| 项 | 内容 |
|----|------|
| 状态 | TESTED |
| 提交 | `d52cae1`、`dd1097b`（HTTP 验收测试） |
| 修改 | `intraday_loop.py`（Trigger 45s / Fast Lane 90s / Evidence 600s 独立 due-time，env 可配；per-lane 连续错误、overall=max；QUOTE_FAILED 等失败态视同 lane 失败；heartbeat 节流 30s 落库、错误轮强制）；`read_worker_heartbeat.py`（新建读取服务）；`strategy_repositories.py`（read_health_events）；`app/api/v3.py`（GET /operations/worker-heartbeat）；`scripts/v3_scheduler.py`（build_health_sink 接线） |
| 正例 | 连续 3 次 Fast Lane 失败 → heartbeat fast_lane_consecutive_errors>=3、consecutive_errors>=3（overall 不被 trigger 成功掩盖）、落库 DEGRADED、HTTP 可见 |
| 负例 | 健康 → HEALTHY、consecutive_errors=0；三 lane interval<=0 → ValueError 拒绝构造 |
| 回归 | test_intraday_loop 16 passed；test_read_worker_heartbeat 6 passed（含 HTTP 层 2 条） |

### R5-08 盘中事件链（§28/§31/§33/§66）

| 项 | 内容 |
|----|------|
| 状态 | TESTED |
| 提交 | `bc7700f` |
| 修改 | `attention_engine.py`（`evaluate_entry_trigger_cancel`：全客观 Trigger 满足→ENTRY_TRIGGER_MET、逼近 1%→NEAR、任一 Cancel→ENTRY_CANCEL_MET、TEXT 不假装判定、suspended/stale/UNTRUSTED→DATA_QUALITY_DEGRADED；`record_structure_changes`：趋势翻转→STRUCTURE_CHANGED 去抖）；`intraday_event_poll.py`（新建：池=现仓+Watchlist 现态，evidence known_at 30min 窗口→record_new_evidence；结构扫描上限 30 券）；`intraday_loop.py`（evidence lane 接线 + evaluate_once 评估 typed triggers/cancels）；`scripts/v3_scheduler.py`（IntradayEventPollService 接线） |
| 正例 | 价 9.41 vs trigger PRICE_ABOVE@9.3 → ENTRY_TRIGGER_MET；9.27 → NEAR；9.0 vs cancel PRICE_BELOW@9.02 → ENTRY_CANCEL_MET（WARNING）；60m 趋势 UP→DOWN → STRUCTURE_CHANGED 一次 |
| 负例 | TEXT-only plan → 零事件；plan 不可解析 → 零事件；窗口外 evidence → 剔除（materiality 阈值门归 engine）；Deep 单券失败 → 跳过不阻断；扫描上限 30 券生效 |
| 安全边界 | AttentionEvent ≠ Trade：只产事件，不改 Decision/持仓状态 |
| 回归 | test_attention_engine 19 passed；test_intraday_event_poll 6 passed；test_intraday_loop 16 passed |

## 3. Final Test Gate（§70）

| 检查项 | 结果 |
|--------|------|
| compileall app tests | OK |
| git diff --check | OK |
| OpenAPI duplicate operation id | 0（`api.openapi()` 共 114 operations） |
| Auth matrix | BearerAuthMiddleware 仅护 /mcp 路径；R5 未改动 auth 面 |
| 完整 tests/v3（非仅新增） | **444 passed, 90 skipped**（基线 396+90 → +48，skip 均为门控） |
| PostgreSQL Integration | 待跑：V3_TEST_DATABASE_URL 未设置（R5-06 新增 SQL 断言待真实库验证） |
| Runtime Scenario Matrix（§59-§66） | 见第 2 节各 Evidence Pack 正例/负例行，对应测试名均在 tests/v3 |
| 部署与真实交易时段验收 | 等外部复验通过后进行 |

> 状态口径：R5-01～R5-08 均 TESTED（实现+单元/集成测试通过）。READY_FOR_EXTERNAL_REVIEW
> 及之后的 CLOSED 判定留给外部复验，不在本轮自行宣布。
