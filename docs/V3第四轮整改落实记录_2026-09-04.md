# V3 第四轮整改落实记录（2026-09-04）

> 依据 `docs/gpt-market_V3_第四轮代码复验报告_2026-09-04.md` §25 指定顺序，
> 完成 R4-01→R4-08 共 8 项修复。**不开新功能**（不接 GLM、不做强势启动、
> 不做 AI Access、不大改 Dashboard、不扩新策略）；§19 RELEASE_MODE vs
> RESEARCH_SHADOW 与 §23 Dashboard 属产品裁决，报告明确"先不要求开发"。

## 1. 完成总表（8/8 = 100%）

| # | 报告条目 | 内容 | 关键实现 | Commit |
|---|---------|------|---------|--------|
| R4-01 | R4-P0-001（§26.2 前视泄漏） | 实时决策上下文只答 NOW | API 层 `_require_current_as_of`（±5 分钟容忍，超差 400 `CURRENT_ONLY_CONTEXT`，历史走 ContextPack/Replay）；服务层 PIT 纪律（未来 Decision/Plan 绝不入选）；`map_quote_snapshot` Future Guard（known_at/event_time > as_of → stale+UNTRUSTED+stale_reason）；`IntradayQuoteSnapshot.stale_reason` 字段 | `9c66189` |
| R4-02 | R4-P0-002（§26.3 stale quote Gate） | Attention 引擎级数据质量 Gate | `evaluate_entry_plan_levels` 入口 gate（suspended / stale / quality==UNTRUSTED）→ 只发 `DATA_QUALITY_DEGRADED`（dedupe `DATA_QUALITY_DEGRADED:{market}:{code}`，reason SUSPENDED/UNTRUSTED_QUALITY/STALE_QUOTE）+ 提前 return；绝不 STOP_HIT/TARGET_HIT/NEAR；Loop 不改——引擎是唯一权威防线 | `a1d5747` |
| R4-03 | R4-P1-003（§28 Fast Lane） | Intraday Fast Lane 接 Runtime | 新建 `IntradayFastLaneService`：全市场 Quote（东财 clist 批量）→ Overlay（FeatureQuery 翻页）→ Scanner（stale 排除）→ Active Pool（EOD/Watchlist/Portfolio/盘中四源合并）→ 前 N 只 Deep 摘要 → Attention（异常/池内 stale DQ）；QUOTE_FAILED 如实上报；`IntradayTriggerLoop` 同间隔跑 Fast Lane；scheduler 完整接线（`V3_FASTLANE_FEATURE_LIMIT`/`V3_FASTLANE_DEEP_LIMIT`）；MCP scan INTRADAY 走只读模式（engine=None，绝不写 Attention） | `deb51da` |
| R4-04 | R4-P1-004（§16 聚合不完整） | Entry Decision Context 聚合补全 | 一次 UoW 会话补 `market_regime`/`feature_eod`（PIT `latest_security_feature`）/`latest_recall`/`latest_raw_opportunity`/`latest_action`/`latest_entry_assessment`/`attention_events`/`data_quality`/`security`；无数据源显式 `NOT_AVAILABLE`+reason（fundamental/evidence）；单 repo 失败降级 UNKNOWN 不拖垮整包；readiness 主链不动 | `4e05fe6` |
| R4-05 | R4-P1-005（§17 权限语义） | Evidence subject allowlist + market-reviews 收回 | `/evidence/` 不再任意 prefix 公开——`PUBLIC_EVIDENCE_SUBJECT_TYPES = {SECURITY, PUBLIC_INDUSTRY, PUBLIC_POLICY}`，其它 subject（POSITION/ACCOUNT/…）一律 MARKET_READ；`/market-reviews`（AI Review 语义）撤出公开列表回归认证 | `022503f` |
| R4-06 | R4-P1-006（§18 Kline provenance） | 每周期 Bar 来源/质量信息 | `IntradayBarSeries` 新增 `source`（实际路径：eastmoney/tencent/aggregate:day:tencent/cache:…）/`upstream_source`（时戳来源）/`known_at`/`quality`/`confidence`/`fallback_used`（主源 eastmoney 之外即 fallback）；UNKNOWN 周期只带 reason | `fbe5258` |
| R4-07 | R4-P2-010（§22 MCP 闭环） | MCP candidate_comparison + INTRADAY scan | 新工具 `v3_candidate_comparison`（按 pack id 精确读 / candidate_set_id 读最近已发布 pack）；build 是落库写操作（API-002），MCP 保持只读、诚实指引走 HTTP POST（V3_WRITE）；INTRADAY scan 已随 R4-03 接线——扫描→比较→深度→决策链闭环 | `e2e717e` |
| R4-08 | R4-P2-008/009（§20/§21） | Worker heartbeat + 优雅收口 + Shanghai 时区 | `IntradayTriggerLoop.heartbeat`（last_success_at/last_error_at/last_error_type/consecutive_errors/plan_count/evaluated/quote_failed/engine_failed/fast lane 状态/provider_health），run_forever 每轮记录、绝不静默；`run_resident` finally 中 await cancelled task + `provider_manager.close()` + `database.close()`；`build_intraday_loop` 返回 (loop, provider_manager)；`_holding_sessions` 交易日归属固定 `Asia/Shanghai` | `909d655` |

## 2. 验证

- **全量回归**：`pytest tests/v3` → **396 passed / 90 skipped**（第三轮末基线 368/89；新增 28 用例全绿）。
- 每项修复均先跑定向测试再提交：entry context（含 PIT guard）、attention engine、fast lane、auth 矩阵、intraday bars/loop、position context、RT-08 MCP。
- `py_compile` 校验 `scripts/v3_scheduler.py`、`app/mcp/v3_tools.py` 等接线文件。

## 3. 报告中"先不要求开发"的项（未在本轮范围）

| 项 | 报告节 | 状态 |
|----|--------|------|
| RELEASE_MODE vs RESEARCH_SHADOW | §19 | 产品裁决待定；当前 mode=V2 下 V3 Recall SKIPPED 是既定裁决 |
| Dashboard 升级为选股看板 | §23 | 报告明确"目前不要求" |
| 接 GLM / 强势启动 / AI Access / 新策略 | §32 边界 | 本轮明确不做 |

## 4. 下一步（报告 §25 顺序）

1. **PostgreSQL Integration**（PostgreSQL 全量回归，需 `V3_TEST_DATABASE_URL`）；
2. **部署**——第四轮代码已全部推送 GitHub，**尚未部署服务器**（遵循此前"不部署生产"惯例，待用户确认）；
3. **真实交易时段验收**——盘中 Worker（含 Fast Lane）+ heartbeat 观测。

## 5. 遗留风险

- 生产服务器 106.13.171.166 后端 8000 此前实测 502（nginx 活、后端未起），且不含第二/三/四轮整改——部署时一并处理；
- Fast Lane 全市场 clist 快照在生产频率（默认 300s 间隔）下的东财限流表现需真实时段观察；`V3_FASTLANE_FEATURE_LIMIT=2000` 特征翻页 10 页 × 200/页；
- `latest_recall/latest_raw_opportunity` 按 200 条最新翻页内客户端过滤，若目标证券不在最新一页则诚实返回 NOT_IN_LATEST_PAGE（不做全量扫描换性能）。
