# V3 第三轮整改落实记录（2026-09-03）

依据：`gpt-market_V3_第三轮代码复验报告_2026-09-03.md`（§11 修复顺序 R3-01→R3-09）。
状态词汇：CLOSED / PARTIAL / NOT_CLOSED / REGRESSION / BLOCKED。

范围裁定（报告 §12/§10，不越界）：不扩新功能；不接 GLM/强势启动/AI Access；
Performance、Replay、Audit 全矩阵、Industry Benchmark 等列为后续再做，不阻塞本轮。

---

## 一、修复总表

| # | 问题 | 严重度 | 修复要点 | 提交 | 状态 |
|---|------|--------|----------|------|------|
| R3-01 | Scheduler boot 顺序错误：ReleaseResolver 消费未初始化的 database | P0 | 新增 `build_database(database_url)` 独立构造（惰性不连接）；`run_once` 顺序 = build_database → ReleaseResolver(uow_factory 消费 database.sessions) → build_orchestrators(database_url, release, database)；复用同一 V3Database 实例 | 59bab9d | CLOSED |
| R3-02 | Release Gate 关闸时连数据事实链一起冻结 | P0 | `DATA_CHAIN_JOB_IDS = (market-data, index-benchmarks, features, evidence-increment)` 永远运行；Gate 只控策略链 full-recall（V2 时 `main.execute(job_ids=DATA_CHAIN_JOB_IDS)` 选段执行）；gate 报表 `{data_chain: EXECUTED, strategy_chain: EXECUTED/SKIPPED, reason}`；catch-up 终端标记随 Gate 切换（V3→full-recall，V2→evidence-increment），pending 永不清零问题消除 | 59bab9d | CLOSED |
| R3-03 | GET 端点藏 build 副作用（API-002 未真关闭） | P0 | GET /candidates/comparison-pack 纯读：comparison_pack_id 精确读 or candidate_set_id → latest_for_candidate_set；GET /stocks/{code}/context-pack 纯读：repo 新增 `latest_for_subject(subject_type, subject_id, as_of)`（known_at ≤ as_of 倒序取 1）；无读键 → 422 提示走 POST；POST = 唯一 build 入口；GET 移除 deprecated 标记 | 19ae27d | CLOSED |
| R3-04 | 公开前缀 /context-packs/ 泄露 POSITION subject 上下文 | P1 | security.py PUBLIC_MARKET_READ_PREFIXES 收敛为仅 /evidence/；/context-packs/{id} 回归 MARKET_READ 认证；公开股票上下文只走 SECURITY-only 的 GET /stocks/{code}/context-pack；auth 测试补 /api/v3/context-packs/1 为 PRIVATE_READ | 19ae27d | CLOSED |
| R3-05 | ExpectedRun schedule 语义未定义：空白/cron 变体一律伪造 16:00 slot | P1 | `profile_schedule_slots(profile, local_day)` 显式契约：缺失/空白 → []（NO_AUTO_SCHEDULE，skipped_no_schedule 计数）；简式 HH:MM 列表 → 逐时刻 slot；5 段 cron 仅支持固定 M H * * *（dom/month 必须 *，minute/hour 纯数字，dow 支持 */列表/范围，0=Sunday）；其余 ValueError → UNSUPPORTED_SCHEDULE 记 errors 绝不伪造；handler 逐 slot 幂等登记 + 单 profile 失败隔离 | 8ead076 | CLOSED |
| R3-06 | V3 实时链路绑死单 EastmoneyProvider，未接 ProviderManager | P1 | ProviderManager（primary/secondary + 有限重试 + 健康降级）接口天然匹配 V3 _MarketProvider Protocol，直接注入；接线 7 处：api/v3.py ×4、mcp/v3_tools.py ×2、scheduler build_intraday_loop ×1；Deep 服务显式 source=legacy-provider（数据可能来自次级 Provider，不再误标 eastmoney） | f39bcab | CLOSED |
| R3-07 | stale/LKG 价也算出确定性 stop/target 触达 | P1 | objective_sell_facts 只在 price_source == REALTIME_QUOTE 且非 quote_stale 时计算 hit；否则 assessment_reason = STALE_REALTIME_QUOTE / NO_REALTIME_PRICE，hit=None；LKG close 只作展示/PnL 参考（比报告建议更严：不止 stale） | be8beb9 | CLOSED |
| R3-08 | Position Deep multi_timeframe 循环内重复调 get_intraday_structure（3× 抓取 + 快照不一致） | P2 | 循环外单次调用，逐 period 取 periods；兼容 dict 与 pydantic IntradayStructure；缺失 period → UNKNOWN + PERIOD_NOT_IN_STRUCTURE；未绑定 deep → DEEP_MARKET_DATA_NOT_BOUND 保持；回归测试断言 deep.calls == 1 | c4aeee6 | CLOSED |
| R3-09 | 重复 OpenAPI operation id read_portfolio_preferences | P2 | 双 @router.get 叠同一函数 → 拆双命名端点共用私有 handler；别名路径 /portfolio-preferences 保留兼容；openapi() 全表 duplicates: NONE | 44c6380 | CLOSED |

---

## 二、测试验证

- 全量回归：`pytest tests/v3 -q` → **368 passed / 89 skipped**（第二轮末基线 355，本轮 +13）。
- 每步定向测试随提交验证通过（test_scheduler / test_phase6_read_api / test_phase6_architecture_gate / test_v3_auth / test_read_position_context / test_position_decision_context / test_intraday_market_data）。
- R3-09 后 openapi() 全路径 operation id 重复集合为空。

## 三、未执行项（按裁定）

- PostgreSQL Integration、生产部署、交易时段真实验收：**BLOCKED by 用户指令**（"不用部署生产"），属后续阶段。
- 后续再做清单：Performance ADD/REDUCE/FINAL_EXIT、Replay Recall/Candidate/Entry Trigger、Audit 全矩阵、Industry Benchmark。

## 四、遗留风险声明

- mode 仍为 V2（effective_v3=false）：Gate 关闸期间数据链照跑，策略链 SKIPPED，属本轮设计预期。
- Deep 服务 source 标签 legacy-provider：真实 ProviderManager 次级降级时不再误导溯源。
- 第二轮复验报告中 API-002 经 R3-03 才真正关闭（见第二轮记录状态表注记）。
