# gpt-market 工作状态

> 最后更新：2026-08-31（Asia/Shanghai）
>
> 当前稳定基线：`main@4e80755` / `v3-phase5-multi-recall-20260831`
>
> 当前状态：开发上下文治理已完成；等待用户确认后再启动 Phase 6

## 当前结论

V3 Architecture Baseline 1.0 保持冻结。Phase 1–5 已完成技术验收，生产仍运行 V1/V2 + Phase2A，V3 默认关闭且未执行生产 Migration。开发上下文治理已经完成，本轮未开发 Phase 6 业务；下一个独立 Task 为 Phase 6 `P6-01`。

普通开发状态已迁移到 [Phase 6 STATUS](phases/phase6/STATUS.md)。历史细节由 Phase 1–5 实施记录、Git 提交和稳定标签承担，本文件不再复制完整流水账。

## 已验收基线

| Phase | 稳定标签/证据 | 状态 |
|---:|---|---|
| 0 | `v2-phase2a-baseline-20260829` | DONE |
| 1 | `v3-phase1-foundation-20260829` / `Phase1基础验收记录` | ACCEPTED |
| 2 | `Phase2行情底座实施记录`：Universe/Bar/Factor 5,551/5,551 | ACCEPTED |
| 3 | `v3-phase3-full-market-features-20260830`：Feature 5,551/5,551 | ACCEPTED |
| 4 | `v3-phase4-core-evidence-20260831`：Core Evidence 真实 PostgreSQL | ACCEPTED |
| 5 | `v3-phase5-multi-recall-20260831`：12 通道、3,492 命中、幂等/性能 | ACCEPTED |

## 最近完成：开发上下文治理

- **Goal**：把跨机器开发规则、最小上下文、Task/Phase 状态和架构红线持久化到 Git；
- **WRITE SCOPE**：`AGENTS.md`、治理/导航/状态文档、Phase Capsule 和轻量治理测试；
- **Forbidden Changes**：业务代码、Migration、API、Provider、Scanner、评分、缓存、MCP 和生产配置；
- **完成条件**：MD/HTML 配对、治理结构检查、最小业务回归、中文提交并推送；
- **停止条件**：提交后停止，不自动开始 Phase 6。

## 阻塞与 UNKNOWN

- 治理输入中的 `known_at <= as_of` 孤立示例与正式时点语义冲突，已按 Baseline 拆分为“记录 `known_at >= fetch_time`；Replay 选择 `known_at <= replay_as_of`”；
- Phase 6 的正式 Schema/Builder Version 尚未由 P6-01 冻结；
- 服务器在 Phase 5 收尾后曾再次出现 SSH banner 超时和公网 502；这是独立运维问题，治理 Task 不修改生产。

## 最近验证

- Phase 5 本地回归：`184 passed, 23 skipped`；
- Phase 5 隔离 PostgreSQL 17 全量回归：`202 passed, 5 skipped`；
- 5,551 只全市场 Recall：12 通道中 6 成功/6 明确 UNAVAILABLE，3,492 命中；READ P95 1.081–12.273ms；
- Phase 5 已合并 GitHub `main@4e80755` 并推送稳定标签。
- 治理结构专项测试：`5 passed`；Guardrail 编号 G-001–G-048 连续唯一，根/局部 AGENTS 和 Phase Capsule 入口完整；
- 完整本地回归：`189 passed, 23 skipped`，两条既有依赖/异步资源警告保留；
- 文档生成与同步检查：47 组 Markdown/HTML 通过；`git diff --check` 通过；本地环境未安装 Ruff，因此未把 Ruff 表述为通过。

## 下一步

1. 停止并等待用户确认；
2. 下一次独立 Task 按 [Task 模板](development/TASK_TEMPLATE.md)启动 `P6-01`，只冻结 Phase 6 Contract 与 0006 Migration 设计；
3. P6-01 完成测试、STATUS、中文提交和推送后停止，不自动开始 P6-02。

## 新环境接手入口

```text
AGENTS.md
docs/ARCHITECTURE_GUARDRAILS.md
docs/development/CONTEXT_POLICY.md
docs/工作状态.md
docs/phases/phase6/{SCOPE,CONTRACTS,ACCEPTANCE,STATUS}.md
```
