# gpt-market 工作状态

> 最后更新：2026-08-30（Asia/Shanghai）
> 当前分支：`codex/phase2-market-data-foundation`
> 当前稳定基线：`v3-phase1-foundation-20260829`
> 状态对应提交：以本文件所在的 `origin/main` 最新提交为准

## 当前结论

V3 架构设计已经冻结为 Baseline 1.0。Phase 1 已验收、合并 `main` 并建立标签 `v3-phase1-foundation-20260829`；用户已授权继续完成 Phase 2–11 全部 V3。当前在独立分支实施 Phase 2 全市场历史底座；继续保持 V1/V2 兼容，不修改其评分规则，线上仍以现有 V1/V2 + Phase2A 部署为运行基线。

## 已完成

- V2/Phase2A 基线提交：`556a335`；
- 基线标签：`v2-phase2a-baseline-20260829`；
- V3 架构经过 v0.1、v0.2 和最终收口 Review；
- 完整架构实施稿：`docs/架构设计实施稿.md` / `.html`；
- V3 设计输入和 V2 历史过程文档已分类归档；
- 开发工作规范和跨电脑/跨模型接手规则已建立；
- README 已提供统一接手入口。
- V3 需求规格说明和系统功能架构已补齐；
- V3 技术架构、数据库逻辑设计、详细设计和功能状态矩阵已补齐；
- `scripts/build_docs_html.py` 已建立 Markdown → HTML 自动生成与 Hash 同步检查；
- V3 历史蓝图与 Review 输入已转换为 Markdown/HTML 并归档；
- 五份核心 V3 设计文档及架构实施稿已完成五项一致性修补；
- 《需求追踪矩阵》已覆盖全部 49 项 P0/P1，并映射 Phase、模块、表/存储、API/Job、测试、状态和证据；
- 《Baseline 一致性修补摘要》已建立，最新审查输入已归档。
- Phase 1 启动检查点已创建并推送：`ca754f3`；
- 已审计配置、Container、Compose、依赖、SQLite/JSONL 和测试结构；
- ADR-0001 已确认 PostgreSQL 17 + SQLAlchemy 2.0 async + asyncpg + Alembic async，V3 默认关闭并与 Legacy 生命周期隔离。
- 已增加 V3 Feature Flag、数据库配置、PostgreSQL Compose Profile、SQLAlchemy async 生命周期和禁用态 Container；
- 默认禁用与配置约束测试已通过，现有 Container 只增加隔离的 V3 生命周期挂点。
- 已建立 Alembic async 环境、统一 SQLAlchemy Metadata、离线 Migration 能力和独立迁移说明；应用启动不会自动运行 Migration。
- 已实现 Canonical JSON/Hash、时区与 `known_at` 契约、AgentTask/AIResultEnvelope、Evidence Contract 和 Task Group Count 状态机；
- 首个 Migration 已定义 Evidence、Task、Agent Envelope 和 Audit 共 9 张基础表，并为 Raw/Evidence/Task/Envelope/Audit 关键事实增加数据库不可变触发器。
- 已在服务器隔离网络和隔离目录完成 Python 3.12 镜像构建及 PostgreSQL 17 真实验收；Upgrade、9 表检查、`known_at`/组计数约束、不可变触发器、Downgrade、Backup/Restore 均通过，未改动生产容器。
- 已完成 Repository Protocol、SQLAlchemy Repository、Unit of Work、Audit Contract 和 AgentTask 注册用例；Task 与 Audit 同事务提交，失败整体回滚。
- Phase 1 技术验收记录已建立；用户已确认验收并授权继续完成全部 V3。
- Phase 1 已合并 `main@f20cc10` 并推送标签 `v3-phase1-foundation-20260829`；Phase 2 分支已创建。
- Phase 2 已建立证券、Universe、Ingestion Run、Bar/Factor Revision、Market Bar 和 Corporate Action 的 Domain/ORM/`0002` 基础；真实 PostgreSQL 17 Upgrade、约束、不可变触发器和降回 Phase 1 已通过。
- Phase 2 Universe 数据层已实现：Legacy 东方财富主源、上交所 + 深交所 + 北交所分页组合的独立官方备用源、覆盖骤降拒绝、显式 Secondary 接管、双源失败 LKG stale 和无 LKG 明确报错。
- Universe Repository 已在单事务内发布 Source/Snapshot/Security/Member/Diff，并可读取最新不可变快照；网络请求不占用数据库事务。
- Phase 2 日 K 单股链路已实现 RAW/QFQ 成对获取、Factor Revision、QFQ-only LIMITED、陈旧门禁、partial 隔离、交易日历驱动的周/月聚合及同事务发布；实现记录见 `docs/Phase2行情底座实施记录.md`。
- Phase 2 历史 Backfill 已实现不可变 Universe 绑定、批次 checkpoint、紧凑高水位/失败游标、失败目标重试、发布后崩溃恢复、已完成运行幂等返回和 `row_version` 并发冲突保护；真实 PostgreSQL 17 端到端重放保持 8 个序列零重复。
- Phase 2 严格交易日历已接入 `exchange_calendars:XSHG`，记录版本与覆盖边界，越界硬失败；已按上交所 2026 春节休市安排验证节后短周聚合，本地及服务器 Python 3.12 均通过。
- Phase 2 Backfill 已支持默认 4、上限 32 的受控并发；网络/发布并行但 checkpoint 单协调器串行，3 并发上限及真实 PostgreSQL 并发重放均通过。
- Phase 2 可执行行情 Job 已接线：官方 Universe PRIMARY、东方财富 SECONDARY，支持 Universe/Backfill/All、run ID 恢复、分段执行和结构化报告；异常扩张超过 LKG 5% 会拒绝。
- Universe/Bar 不可变 Hash 已统一成员排序、UTC datetime 和 DDL 数值精度，5,551 成员 PostgreSQL 存储 Hash 与重建 Hash 一致。

## 正在进行

- 将已可执行的 Universe/Backfill Job 接入 Compose Worker/Scheduler，并补全持续运行观测；
- 实现全市场 Raw Bar/Factor Revision、增量更新和本地周/月聚合持久化。
- 接入可执行 Job 和结构化覆盖率报告后，执行全市场 5,000+ 正式跑批。

## 未开始

- Phase 2 及后续所有 V3 实施阶段；
- 生产环境 V3 PostgreSQL 部署和正式 Migration；
- V3 API、Worker、Feature、Recall、Evidence Ingestion、AI Import 和 Portfolio 实现；
- 服务器 V3 部署。

## 阻塞与 UNKNOWN

- Scheduler 技术实现仍待进入对应 Phase 后确认；
- 当前日历版本覆盖至 2026-12-31；进入 2027 年前必须升级数据包并按交易所公告复验，越界时系统会显式失败；
- 部分历史数据可能只有前复权序列，Point-in-Time 精度需在 Phase 2 实测；
- 本状态不代表当前服务器部署版本已经包含 V3，服务器仍以现有部署事实为准。

## 最近验证

- 架构 Markdown 与离线 HTML 均包含 Baseline 1.0；
- AIResultBundle 已采用逻辑 `atomic_group`，不存在整包 `ALL_OR_NOTHING` 旧语义；
- Bundle 示例 JSON 已通过解析；
- 当前文档整理没有修改行情、扫描、评分、Provider、缓存、MCP 或 Web API 业务逻辑。
- 文档生成检查：29 组 Markdown/HTML 内容 Hash 同步（包含根 README、CONTRIBUTING 与 `docs`）；
- 需求追踪检查：需求规格中的 49 项 P0/P1 与矩阵 49 项一一对应，无缺失、无重复、无额外编号；
- 术语检查：正式文档不存在 `corporate_action_adjustments` 或整包 `ALL_OR_NOTHING` 旧设计；
- 当前本地 `.venv` 使用 Python 3.13；Phase 1 应用镜像已在服务器使用 Python 3.12 构建通过。
- Phase 1 启动前全套测试：`72 passed, 5 skipped`；
- 本机 Docker/Compose 可用；仓库内不存在既有 SQLAlchemy/Alembic/PostgreSQL 实现；
- SQLAlchemy/Alembic/asyncpg 选型已对照官方 async 文档核验。
- V3 数据库基础单元与 Legacy 回归：`75 passed, 5 skipped`；
- Compose YAML 结构检查通过；本机 Docker CLI 可用但守护进程未运行，改在服务器隔离环境完成 Python 3.12 镜像和 PostgreSQL 17 验收。
- Alembic 环境、Metadata 和离线 SQL 验证通过；全套测试：`77 passed, 5 skipped`。
- Contract、Task 状态、Metadata 与首个 Migration 离线验证通过；全套测试：`83 passed, 5 skipped`；
- 真实 PostgreSQL 17 验收：Upgrade 至 `0001_phase1_foundation` 成功，`v3` 下 9 张表齐全；不可变触发器、`known_at >= fetch_time` 和任务组计数不变量均实际拒绝非法写入。
- Migration 可逆性与灾备验收：Downgrade 至 `base` 后 `v3` schema 和版本记录均为空；`pg_dump` 恢复后 9 张表、版本号、测试数据和不可变触发器均正常。
- PostgreSQL 集成测试入口：`tests/v3/test_postgres_integration.py`，由 `V3_TEST_DATABASE_URL` 显式启用；未配置真实数据库时跳过，禁止伪装为已通过。
- 当前全套本地回归：`86 passed, 8 skipped`；3 项 PostgreSQL 集成测试因本机未配置 `V3_TEST_DATABASE_URL` 明确跳过，其等价断言已在服务器隔离 PostgreSQL 17 上通过。
- Repository/Audit Fake 测试覆盖同事务提交、顺序幂等和 Audit 失败回滚；真实 PostgreSQL 进一步通过相同 Task ID 内容冲突及 8 并发幂等验证。
- Phase 2 Domain、Metadata、Migration 与 Legacy 完整回归：`90 passed, 9 skipped`；新增 PostgreSQL 集成项在本机无真实库时明确跳过，等价断言已在服务器 PostgreSQL 17 通过。
- 官方备用 Universe 在服务器实际抓取 5,551 只：上交所 2,315、深交所 2,897、北交所 339，解析数、唯一代码数均等于上游声明数，覆盖率 100%，耗时 5.309 秒；北交所全部为当前 `920xxx` 代码。深交所 XLSX 虽返回 HTTP 200 但只有表头，已拒绝该伪成功并采用受控 JSON 分页。
- Universe fallback 单测覆盖主源失败后 Secondary 接管、覆盖骤降后 LKG、无 LKG 显式失败和主源正常发布；真实 PostgreSQL 17 验证两版快照、最新读取及 `ADDED/CHANGED/REMOVED` 差异各 1 条。
- Universe 单元完成后的全套本地回归：`98 passed, 10 skipped`；其中 5 项 PostgreSQL 集成测试未配置本地数据库而明确跳过，新增 Repository 集成项已在服务器隔离 PostgreSQL 17 通过。
- 日 K 真实探针 20/20 成功：东方财富 0、腾讯接管 14、新浪接管 6；全部取得 300 日 RAW/Factor/FULL Revision，最后 Bar 均为 2026-08-28，平均 1.057 秒，4 并发墙钟 5.872 秒。
- Bar Bundle 在隔离 PostgreSQL 17 通过 nullable 成交额、Factor 外键、首次发布、重复零新增和不可变约束；全套本地回归 `114 passed, 11 skipped`。
- Backfill/Resume V3 专项测试 `43 passed, 7 skipped`；服务器隔离 PostgreSQL 17 端到端验证 Universe 目标读取、2/2 checkpoint、日周月 RAW/QFQ 发布及同 run 重放零新增。
- 严格交易日历对照上交所 2026 春节公告验证 2 月 23 日休市、2 月 24 日开市和节后四交易日短周；服务器 Python 3.12 日历/聚合测试 `6 passed`。
- Backfill 6 目标并发测试确认配置 3 时最大活跃数为 3，两个批次 checkpoint 完整且成功态游标保持常量大小；服务器真实 PostgreSQL 默认并发发布和同 run 重放 `1 passed`。
- 东方财富与官方 Universe 集合实测：主源 5,905、官方 5,551，主源多出的 354 条包含定转、旧代码及退市证券，官方无反向缺口；据此调整为官方 PRIMARY 并新增 5% 异常扩张门禁。
- Phase 2 Job 在全新 PostgreSQL 库从 `0001 -> 0002` 冷启动：官方 5,551 发布成功，首段 20/20；同 run 恢复至 40/40，两个分段均 0 失败；数据库重建 Hash 等于存储 Hash。
- 生产 `market-mcp` 在 Phase 1/2 隔离验收期间持续为 `healthy`；Phase 1 已合并，生产 V3 仍未启用。

## 下一步

1. 将已验收 CLI Job 接入隔离 Compose Worker/Scheduler；
2. 使用现有 run ID 继续执行 5,551 全市场覆盖率与恢复验收；
3. 接入 Corporate Action 原始事实，并建立交易日历年度升级门禁；
4. 用真实全市场任务验证覆盖率、修订、聚合和恢复能力。

当前 V3 回滚点为标签 `v3-phase1-foundation-20260829`；Phase 2 未验收前不合并 `main`、不启用生产 V3。

## 接手关键文件

```text
README.md
docs/架构设计实施稿.md
docs/开发规范.md
docs/工作状态.md
docs/README.md
app/
tests/
```

默认无需阅读 `docs/archive/`。需要追溯 V1/V2 行为或 V3 设计决策时，再根据 `docs/README.md` 定向查看。
