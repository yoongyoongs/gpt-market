# gpt-market 工作状态

> 最后更新：2026-09-02（Asia/Shanghai）
> 当前分支：`main`
> 当前生产应用镜像基线：`main@0310a49`（RC-03 已部署；RC-04A/B 代码完成待随 RC-04 组部署）
> 策略发布状态：`mode=V2`（V3 API/数据库已启用，V3 策略尚未激活）

## 当前结论

V3 架构设计已经冻结为 Baseline 1.0。Phase 1–11 已完成技术验收；Phase 7–11 在全新隔离 PostgreSQL 17 上完成 Domain、Migration、Repository、并发、OpenAPI 和全项目回归，最终结果为 `243 passed, 5 skipped, 2 warnings`。2026-09-01 已将验收提交合并 `main` 并部署生产 V3 API、PostgreSQL 17 与 Phase 2 Worker；策略 Release State 继续保持 `mode=V2`，尚未激活 V3 策略。

2026-09-01 已根据外部 V3 最终评审进入 Product Closure 整改。整改前基线为 `main@3c8a078`、Migration Head `0011_strategy_stabilization`；Phase 1–11 历史技术验收继续有效，但不再用“Phase 11/11”表示产品已 100% 闭环。RC-01 安全边界与 RC-02 Correction Chain/Opening 基线/成本法契约/执行偏差/公司行动 Match/Projection Verify（RC-02A–D）均已达到 `TECH_ACCEPTED`，当前首要未完成项为 RC-03 正式全链路 Orchestrator；详细状态和逐批证据统一记录在 `docs/V3最终收口实施记录.md`。

RC-00 已完成：建立并推送整改前标签 `v3-pre-product-closure-20260901`，校准需求追踪状态，并增加 `scripts/test_suite.py` 统一离线、V3 和 PostgreSQL 测试入口。PostgreSQL 模式必须显式使用 `V3_TEST_DATABASE_URL`，不会默认连接生产数据库；完整离线回归为 `224 passed, 29 skipped, 2 warnings`。

RC-01A 已完成：V3 市场 READ 保持可配置公开；Portfolio READ 和所有 WRITE 已按 `PORTFOLIO_READ/V3_WRITE` 保护；Strategy Activate/Rollback 使用独立 `STRATEGY_ADMIN`。专项回归 `8 passed, 2 warnings`，完整离线回归 `230 passed, 29 skipped, 2 warnings`。服务端 Principal 与命令字段绑定以及生产/Nginx 止血验证仍在 RC-01 后续批次，当前不得把 RC-01 标为完成。

RC-01B 已完成：所有已有 `actor_id/actor_type/confirmed_by` 的关键 V3 写命令均由认证 Principal 覆盖，客户端请求体不能再伪造操作身份；PENDING Adjustment 不会被误标为已确认。专项回归 `22 passed, 1 warning`，完整离线回归 `233 passed, 29 skipped, 2 warnings`。RC-01 仍需完成生产 Secret/Nginx 配置和公网 401/403/200 验收。

RC-01C 已完成生产部署：应用与 Phase 2 Worker 使用 `gpt-market:main-0d090da`，Nginx 继续从公网 80 端口代理到 `127.0.0.1:8000` 并透传 Authorization；未认证 Portfolio/WRITE 为 401，普通 WRITE Token 调 Strategy Activate 为 403，管理员 Token 可进入业务校验，公开 V3 市场 READ、V3/V2 看板、Legacy Quote 和 Health 均为 200。Secret 仅保存在服务器 `0600` 环境文件中，策略仍为 `mode=V2`。RC-01 已标记 `TECH_ACCEPTED`。

RC-03 已完成并部署：生产迁移 Head 为 `0013_orchestrator_job_runs`，生产为镜像 `main@0310a49`，Worker 已切换统一调度器并以 `--once` 完成全链路真实验证（主链 5553/5553、维护链 SUCCEEDED、四条 Job 运行记录落库）。正式 Orchestrator（`app/v3/jobs/orchestrator.py`）实现 Job 图拓扑编排、每 Job 运行落库 `v3.orchestrator_job_runs`（迁移 `0013`）、按交易日幂等跳过、失败下游 SKIPPED/独立分支继续、advisory lock 防并发；统一调度器 `scripts/v3_scheduler.py` 组装收盘后主链（market-data → features）与每日维护链（corporate-action-match、projection-verify）。全新 PostgreSQL 17 回归 `220 passed, 2 warnings`，离线 `251 passed, 47 skipped, 2 warnings`。

RC-04A 已完成（待随 RC-04 组部署）：`weekly_state`/`weekly_trend_state` 字段统一为后者，Feature Run 按同一 as_of 加载已发布 QFQ 周 K Revision 真实计算周趋势状态（MA10W/MA30W 比率 + 斜率：UP/DOWN/BASE/RANGE，周历史不足 UNKNOWN），"周底日强" Recall 通道恢复可用；全新 PostgreSQL 回归 `224 passed`，离线 `255 passed`。

RC-04B 指数基准部分已完成（待随 RC-04 组部署）：新增指数基准事实链（迁移 0014）与六大量价基准目录（沪深300/中证500/中证1000/上证/深证/创业板），Feature Run 按 as_of 点时加载基准 revision 确定性计算 20 日收益注入 `relative_index_strength`，manifest 保存基准 id/revision id/known_at/source/calculation version；调度器主链更新为 market-data → index-benchmarks → features。行业基准因无可靠分类数据源保持 null 不猜测。全新 PostgreSQL 回归 `231 passed`，离线 `261 passed`。

RC-04C Regime 第一层已完成（待随 RC-04 组部署）：Market Regime 的主要指数状态接入 RC-04B 基准事实（UP/DOWN/RANGE 冻结阈值 ±2%，缺失保持 UNKNOWN+reason），全市场成交额补 coverage；涨停连板/行业轮动/风格因子无源维持显式 UNKNOWN。全新 PostgreSQL 回归 `232 passed`，离线 `262 passed`。

RC-02A 已完成并部署：生产 Migration Head 为 `0012_trade_correction_chain`；Trade Correction 已建立确定 Sequence、累计 Patch、前后状态 Hash、Reverse 终态和并发追加保护。全新 PostgreSQL 17 V3 回归 `189 passed`，已有 3 条 Correction 的降级再升级回填为 `3/3`，Projection 删除后重建结果一致。生产迁移前 `pg_dump`、旧镜像和旧容器回滚点均已保留，公网 V1/V2/V3 与 401 安全边界验收通过。

## 已完成

- V2/Phase2A 基线提交：`556a335`；
- 基线标签：`v2-phase2a-baseline-20260829`；
- V3 架构经过 v0.1、v0.2 和最终收口 Review；
- 完整架构实施稿：`docs/架构设计实施稿.md` / `.html`；
- V3 设计输入和 V2 历史过程文档已分类归档；
- 开发工作规范和跨电脑/跨模型接手规则已建立；
- README 已提供统一接手入口。
- V3 需求规格说明和系统功能架构已补齐；
- V3 技术架构、数据库逻辑设计、详细设计和功能状态矩阵已补齐；
- 正式文档统一以 Markdown 为单一来源，同名 HTML 副本已清理；
- V3 历史蓝图与 Review 输入已转换为 Markdown 并归档；
- 五份核心 V3 设计文档及架构实施稿已完成五项一致性修补；
- 《需求追踪矩阵》已覆盖全部 49 项 P0/P1，并映射 Phase、模块、表/存储、API/Job、测试、状态和证据；
- 《Baseline 一致性修补摘要》已建立，最新审查输入已归档。
- Phase 1 启动检查点已创建并推送：`ca754f3`；
- 已审计配置、Container、Compose、依赖、SQLite/JSONL 和测试结构；
- ADR-0001 已确认 PostgreSQL 17 + SQLAlchemy 2.0 async + asyncpg + Alembic async，V3 默认关闭并与 Legacy 生命周期隔离。
- 已增加 V3 Feature Flag、数据库配置、PostgreSQL Compose Profile、SQLAlchemy async 生命周期和禁用态 Container；
- 默认禁用与配置约束测试已通过，现有 Container 只增加隔离的 V3 生命周期挂点。
- 已建立 Alembic async 环境、统一 SQLAlchemy Metadata、离线 Migration 能力和独立迁移说明；应用启动不会自动运行 Migration。
- 已实现 Canonical JSON/Hash、时区与 `known_at` 契约、AgentTask/AIResultEnvelope、Evidence Contract 和 Task Group Count 状态机；
- 首个 Migration 已定义 Evidence、Task、Agent Envelope 和 Audit 共 9 张基础表，并为 Raw/Evidence/Task/Envelope/Audit 关键事实增加数据库不可变触发器。
- 已在服务器隔离网络和隔离目录完成 Python 3.12 镜像构建及 PostgreSQL 17 真实验收；Upgrade、9 表检查、`known_at`/组计数约束、不可变触发器、Downgrade、Backup/Restore 均通过，未改动生产容器。
- 已完成 Repository Protocol、SQLAlchemy Repository、Unit of Work、Audit Contract 和 AgentTask 注册用例；Task 与 Audit 同事务提交，失败整体回滚。
- Phase 1 技术验收记录已建立；用户已确认验收并授权继续完成全部 V3。
- Phase 1 已合并 `main@f20cc10` 并推送标签 `v3-phase1-foundation-20260829`；Phase 2 分支已创建。
- Phase 2 已建立证券、Universe、Ingestion Run、Bar/Factor Revision、Market Bar 和 Corporate Action 的 Domain/ORM/`0002` 基础；真实 PostgreSQL 17 Upgrade、约束、不可变触发器和降回 Phase 1 已通过。
- Phase 2 Universe 数据层已实现：Legacy 东方财富主源、上交所 + 深交所 + 北交所分页组合的独立官方备用源、覆盖骤降拒绝、显式 Secondary 接管、双源失败 LKG stale 和无 LKG 明确报错。
- Universe Repository 已在单事务内发布 Source/Snapshot/Security/Member/Diff，并可读取最新不可变快照；网络请求不占用数据库事务。
- Phase 2 日 K 单股链路已实现 RAW/QFQ 成对获取、Factor Revision、QFQ-only LIMITED、陈旧门禁、partial 隔离、交易日历驱动的周/月聚合及同事务发布；实现记录见 `docs/Phase2行情底座实施记录.md`。
- Phase 2 历史 Backfill 已实现不可变 Universe 绑定、批次 checkpoint、紧凑高水位/失败游标、失败目标重试、发布后崩溃恢复、已完成运行幂等返回和 `row_version` 并发冲突保护；真实 PostgreSQL 17 端到端重放保持 8 个序列零重复。
- Phase 2 严格交易日历已接入 `exchange_calendars:XSHG`，记录版本与覆盖边界，越界硬失败；已按上交所 2026 春节休市安排验证节后短周聚合，本地及服务器 Python 3.12 均通过。
- Phase 2 Backfill 已支持默认 16、上限 32 的受控并发；网络/发布并行但 checkpoint 单协调器串行，3 并发上限及真实 PostgreSQL 并发重放均通过。
- Phase 2 可执行行情 Job 已接线：官方 Universe PRIMARY、东方财富 SECONDARY，支持 Universe/Backfill/All、run ID 恢复、分段执行和结构化报告；异常扩张超过 LKG 5% 会拒绝。
- Universe/Bar 不可变 Hash 已统一成员排序、UTC datetime 和 DDL 数值精度，5,551 成员 PostgreSQL 存储 Hash 与重建 Hash 一致。
- 东方财富历史 K 已增加连续失败熔断与冷却探测，熔断期间快速进入腾讯/Sina fallback，避免全市场任务产生上万次已知无效请求。
- Phase 2 官方 Universe 与东方财富状态组合 Provider 已验收：官方成员 5,551，东方财富只补充停牌/ST 状态并排除 354 条额外污染证券；最终停牌 4 只。
- Phase 2 全市场日 K 最终完成 5,551/5,551、失败 0；腾讯 5,122、Sina 429，全部具备 RAW/QFQ/Factor/FULL，周线 5,551，完整月线 5,534。
- 覆盖检查已改为单次批量查询并按上海交易日比较；全市场已覆盖重放 12.966 秒完成且不再次调用行情 Provider。
- Corporate Action 已完成分页 Provider、标准化、语义 Hash、幂等和更正链；真实抓取 8,793 个逻辑事件，重复运行新增 0、unchanged 8,793。
- Bar/Factor/Corporate Action 均已建立不可变 `supersedes` 修订链，真实 PostgreSQL 已验证。
- Phase 2 Scheduler、独立 Compose `v3-worker` Profile、上海时区日调度、原子报告和 PostgreSQL advisory lock 已实现并通过配置、竞争锁和一次性路径验收。
- Phase 3 数据输入收口已补齐 HFQ：由 RAW 和 QFQ Factor 确定性派生并绑定同一 Factor Revision；旧库只有 QFQ 时不再判定覆盖完整，会自动触发补算。RAW/QFQ/HFQ 发布、幂等、修订链和上海交易日门禁已在真实 PostgreSQL 17 通过。
- Phase 3 `0003_full_market_features`、Feature Domain/Calculator/Repository、可重放 Feature Run、白名单 Universe Query、事实型 Market Regime、READ API 和独立 Job 已实现。
- Feature 统一绑定已发布 QFQ DAY Revision；长周期数据不足、行业/指数缺失和事实维度不可用均显式 `null/UNKNOWN`，不伪造数据，不引入 V2 评分或自动交易。
- 空 PostgreSQL 17 已成功升级至 `0003`；2 只证券 × 260 日 K 的原子发布、幂等、Cursor、Regime 和不可变触发器集成验收通过；全项目真实库回归 `154 passed, 5 skipped`。
- Phase 3 全市场预验收发现非整比 HFQ 价格精度导致 Hash 漂移，已改为哈希前显式规范化并用真实 `600288` 验证；同时补齐无 LKG 冷启动的 5,700 成员绝对上限，官方失败时不再接受东方财富 5,905 条污染集合。
- Phase 3 隔离验收库回填 5,551/5,551、失败 0：日/周 RAW/QFQ/HFQ 各 5,551，月各 5,534，共 6,169,716 根 Bar、1,636,526 条 Factor。
- Phase 3 正式 Feature Run 5,551/5,551、失败 0、耗时 180.679 秒、峰值内存约 291 MiB；3/5/10/20/60/120/250 日收益覆盖依次为 5,550/5,549/5,543/5,534/5,511/5,469/5,397。
- Phase 3 查询 30 次实测 P95 为 6.511–14.688 ms；字段白名单、过滤、排序、Cursor、原子发布和不可变记录通过。特征覆盖分母已按实际 28 个字段修正。
- 相同 Universe、`as_of`、Feature Version 和 Revision Set 全市场重跑返回原 Run，数据库保持 1 个 Run、5,551 条 Feature，无重复发布。
- 腾讯 5,122 只与 Sina 429 只的历史日 K 均无可靠成交额；`amount` 和 Regime Turnover 明确缺失。指数/行业基准未绑定时相对强度保持 `null/UNKNOWN`，不估算、不伪造。
- Phase 3 已合并 `main@39058fc`，稳定标签 `v3-phase3-full-market-features-20260830` 已推送 GitHub。
- Phase 4 `0004_evidence_ingestion` 已补齐 Source/Raw/Evidence 扩展、Fetch Run、不可变 Parse Attempt、Entity Link、Relation、Conflict Set/Member；Raw/Evidence 去重约束保留跨来源独立事实。
- Phase 4 Raw 先提交再 Parse 的最小闭环已实现；Opinion 不升级 Fact、Untrusted、时点 Retrieval、Decay 和低置信 Link 契约已落地。
- Phase 4 Fetch Run 已支持游标 checkpoint、分段恢复、终态重放、Provider 失败终态和 `row_version` 并发冲突保护。
- Phase 4 已实现规范 Payload 精确/近似 Dedup Relation、同 Claim 多值 Conflict Set、全成员保留和按来源优先级/置信度/时点的临时选择解释。
- Phase 4 PostgreSQL 17 `0003 -> 0004 -> 0003 -> 0004` 双往返通过；配置真实库的完整回归 `186 passed, 5 skipped`。
- Phase 4 首批 Core Provider 已接入巨潮公告、东方财富财务/业绩预告/快报、中国政府网政策和东方财富基础财经新闻；能力 Registry、按源限流、重试和结构硬校验已实现。
- 七个真实入口 Fetch/Parse 探针全部请求成功；真实 PostgreSQL 端到端落库共 29 条 Raw/29 条 Evidence、失败 0；当前完整回归 `186 passed, 5 skipped`。
- 上交所官方公告备用源已实测 2 日窗口 1,931 组；巨潮入口增加 A 股代码硬过滤，债券/基金不会再被通用市场映射误标为证券 Evidence。
- Registry Job 已支持真实 fallback、`PARTIAL` 后接管、能力级失败隔离、全源交叉采集、分段恢复参数和逐源计数；3 只真实证券隔离 Job 全能力成功。
- Phase 4 已基于 Published Universe 建立 Security 名称/代码目录，支持显式 Industry Catalog 和 Market 别名；唯一匹配为 `CONFIRMED`，同名歧义只落 `CANDIDATE`，解析器明确 Link 优先。
- Evidence READ 已提供 `GET /api/v3/evidence/{subject_type}/{subject_id}`，支持 `as_of`、Evidence/Source 类型、有效相关性、Candidate 开关和 Limit，并返回冲突状态及逐类型 `AVAILABLE/UNKNOWN` Coverage。
- 真实 PostgreSQL 已验证 Link 查询、Candidate 默认隔离和过滤；50 条结果连续 100 次读取平均 7.438 ms、P95 10.385 ms、最大 11.195 ms。
- Phase 4 完整回归 `186 passed, 5 skipped`；2 条既有依赖/异步资源警告保留，相关文件 Ruff 与 `git diff --check` 通过。
- Phase 4 已合并 `main@afd1856` 并推送稳定标签 `v3-phase4-core-evidence-20260831`。
- Phase 5 `0005_multi_recall_foundation` 已建立 Channel/Run/Result、Raw Opportunity、未来观察和 Recall Miss 六张不可变表；PENDING 未来泄漏和未成熟评价由 Domain/DDL 双层拒绝。
- Phase 5 PostgreSQL 17 `0004 -> 0005 -> 0004 -> 0005` 双往返及全新库 `base -> 0005` 通过。
- Phase 5 已实现 9 个可解释 Feature Channel、缺字段/异常隔离、原子 Run/Result/Raw/全 Universe Observation、稳定 Cursor Recall/Raw READ 和可执行 Job。
- 成熟 Feature 实际 Job 为 6/9 通道成功，周线/指数/行业输入缺失的 3 个明确 `UNAVAILABLE`；相同输入重放返回同一 Run，未来 Feature 在发布 Channel 前被拒绝。
- 真实 PostgreSQL 2 证券闭环为 2 Result、2 Raw、6 PENDING Observation，UPDATE Trigger 实际拒绝；全新测试库完整回归 `197 passed, 5 skipped`。
- Phase 5 已补齐财务改善、业绩拐点和官方催化事件 3 个 Evidence Channel，共 12 个首批通道；规则只读取明确结构化字段或官方事件词并返回 Evidence ID。
- 12 通道真实 Job 在缺输入夹具上为 6 成功/6 `UNAVAILABLE`、Coverage 0.5；第三个全新 PostgreSQL 库完整回归 `199 passed, 5 skipped`。
- 修正 Observation append-only 闭环：PENDING 不再与终态争用同一唯一记录，MATURED/UNAVAILABLE 必须通过 `supersedes_observation_id` 追加且一个 PENDING 最多一个终态。
- 新增 `MatureRecallObservationsService`、Outcome Provider 契约、阈值版本化基础分类和 `GET /api/v3/recalls/misses` Cursor READ；Outcome 获取不占用数据库事务。
- 新增收益一致性、Outcome 完整性、不可用原因、重复执行零新增和异常未召回分类测试；本地完整回归 `184 passed, 23 skipped`，2 条既有警告保留。
- PostgreSQL 集成回归已通过“Recall 仅命中 1/2，但 Full Universe 仍返回 2/2”、追加成熟链、三 Horizon Miss READ 和幂等；修改后的 `0005` 在隔离 PostgreSQL 17 完成 `0003 -> 0005 -> 0003 -> 0005`，5,551 条 Feature 全程保留。
- Phase 5 独立空库完整回归 `202 passed, 5 skipped`；真实 5,551 证券 Run 为 12 通道中 6 成功/6 明确 `UNAVAILABLE`，命中 3,492，发布 5,726 Result、3,492 Raw 和 16,653 PENDING Observation。
- Phase 5 全市场首次执行 14.971 秒，幂等重放 3.539 秒且返回同一 Run；2,059 只未命中证券仍可由 Full Universe Query 读取。20 次 READ P95 为 Feature 12.273ms、Raw 11.858ms、Result 10.600ms、Miss 1.081ms。
- Phase 5 已完成技术验收；稳定标签为 `v3-phase5-multi-recall-20260831`。该阶段当时未启用生产 V3，现已统一迁移至 `0011`。
- Phase 6 P6-01 已冻结 Candidate Comparison、Context Pack、Task Profile/Expected Run/Task Run 的 Domain 与 Repository Contract，提交为 `044da58`，状态提交为 `1585be0`。
- Phase 6 P6-02 已实现完整 `0006_context_task_foundation` 和 CandidateComparisonPack/Member PostgreSQL Repository；检查点提交为 `e669cac`，PostgreSQL 17.11 验收通过。
- Phase 6 的契约、迁移设计和任务状态已集中到 `docs/Phase6上下文任务实施记录.md`，不再使用 Phase Capsule、Context Policy 或多层 Task 治理。

## 本轮完成

- 新增只读 `GET /v3/dashboard` 人工行情特征看板：只读取最新 Published Feature Run/Market Regime，支持市场、排序和数量筛选；不调用 Provider、不写 Candidate Pack、不产生统一评分；空库立即返回初始化页。
- V3 看板专项 `4 passed`；生产 Phase 2 首轮暴露 RAW 比 QFQ 多交易日时 HFQ 派生按 RAW 日期直接索引 Factor 的 `KeyError`，已收紧完整对齐条件：不完整序列明确降为 `LIMITED` 且不生成 HFQ，不伪造缺失因子。
- 上述错位日期回归与全项目本地测试为 `224 passed, 29 skipped, 2 warnings`；修复镜像已部署并使用并发 4 重试首轮失败项。
- 生产 Phase 2 最终取得 Universe `5,553/5,553`、K线 `5,551/5,553`（99.96%）；`601123`、`301697` 经三源独立重试仍为空，明确保留 `UNAVAILABLE`。
- 生产 Phase 3 Feature Run 已发布 `5,551/5,553`，Market Regime 同步生成；公网 `/v3/dashboard`、Feature JSON 和 Regime JSON 均为 HTTP 200，看板实测约 `0.43s`。
- 生产 Phase 2 Worker 并发已从默认 16 固定为 4；`.env` 已建立部署前备份，Worker 重建后环境值与 Health 验证通过，避免全市场任务再次挤占公网服务。
- Phase 7–11 专项验收 `17 passed`：覆盖 29/30 Atomic Group、并发超卖、手工持仓 Review 不成交、Replay BLOCKED、Shadow/容量/人工激活/自动回滚和 OpenAPI 安全边界；
- PostgreSQL 17 从空库 `base -> 0011`、`0011 -> 0006 -> 0011` 往返成功，五阶段代表表按预期删除/重建，检测到 66 个不可变 Trigger；
- 全项目 Python 3.12 + PostgreSQL 17 回归 `243 passed, 5 skipped, 2 warnings`；本地无数据库回归 `219 passed, 28 skipped, 2 warnings`；
- 验收发现并修复 Canonical Hash 不支持 UUID 字典键/Decimal、Position Projection 不复核 Hash、Release Environment 超长触发 DB 500 三项缺陷；
- 详细证据见 `docs/Phase7-11技术验收报告.md`；验收提交已快进合并并推送至 `main@c25f6e4`。
- 2026-09-01 已完成生产部署：PostgreSQL 17 迁移至 `0011_strategy_stabilization`，V3 API 与 Phase 2 Worker 已启用；V1/V2 与公网 Nginx 入口保持正常。
- 生产烟测通过 Health、Quote、日 K、V2 Scan、V3 Strategy List 与 Release State；`v3` schema 为 81 张表、65 张不可变表，Migration 后备份与旧容器/旧镜像回滚点均已保留。
- V3 策略发布状态仍为 `mode=V2`，没有激活空策略、没有连接券商，也没有自动交易。

## 未开始

- 生产 V3 全市场历史数据正式回填、Feature/Recall/Evidence 数据生产与覆盖验收；
- 正式 Strategy Version、Guardrail、Shadow/容量观察和人工激活流程；
- 生产 PostgreSQL 定时备份、保留周期与恢复演练自动化。

## 阻塞与 UNKNOWN

- 当前日历版本覆盖至 2026-12-31；进入 2027 年前必须升级数据包并按交易所公告复验，越界时系统会显式失败；
- 当前验收全市场数据均取得 RAW/QFQ/Factor/FULL；其他时间或上游只能返回 QFQ 时仍按设计降为 `LIMITED`；
- 当前行情 fallback 能保证价格/成交量序列，但不能补齐可靠历史成交额；成熟样本特征覆盖为 25/28，缺失字段均在记录中显式标注；
- 当前全市场 Recall 的周底日强、相对指数、相对行业和三类 Evidence 通道因历史输入缺失明确 `UNAVAILABLE`；Coverage 0.5 是通道执行覆盖，不是证券行情覆盖，也不应被伪装为零命中；
- 生产 V3 数据库当前为新库，API 空列表是尚未生成业务数据，不代表接口故障；
- 生产策略 Release State 保持 `mode=V2`，V3 API 已部署不等于 V3 策略已激活。

## 最近验证

- 架构 Markdown 与离线 HTML 均包含 Baseline 1.0；
- AIResultBundle 已采用逻辑 `atomic_group`，不存在整包 `ALL_OR_NOTHING` 旧语义；
- Bundle 示例 JSON 已通过解析；
- 当前文档整理没有修改行情、扫描、评分、Provider、缓存、MCP 或 Web API 业务逻辑。
- 文档格式检查：正式文档只保留 Markdown；运行时 `app/templates/live_dashboard.html` 继续保留；
- 需求追踪检查：需求规格中的 49 项 P0/P1 与矩阵 49 项一一对应，无缺失、无重复、无额外编号；
- 术语检查：正式文档不存在 `corporate_action_adjustments` 或整包 `ALL_OR_NOTHING` 旧设计；
- 当前本地 `.venv` 使用 Python 3.13；Phase 1 应用镜像已在服务器使用 Python 3.12 构建通过。
- Phase 1 启动前全套测试：`72 passed, 5 skipped`；
- 本机 Docker/Compose 可用；仓库内不存在既有 SQLAlchemy/Alembic/PostgreSQL 实现；
- SQLAlchemy/Alembic/asyncpg 选型已对照官方 async 文档核验。
- V3 数据库基础单元与 Legacy 回归：`75 passed, 5 skipped`；
- Compose YAML 结构检查通过；本机 Docker CLI 可用但守护进程未运行，改在服务器隔离环境完成 Python 3.12 镜像和 PostgreSQL 17 验收。
- Alembic 环境、Metadata 和离线 SQL 验证通过；全套测试：`77 passed, 5 skipped`。
- Contract、Task 状态、Metadata 与首个 Migration 离线验证通过；全套测试：`83 passed, 5 skipped`；
- 真实 PostgreSQL 17 验收：Upgrade 至 `0001_phase1_foundation` 成功，`v3` 下 9 张表齐全；不可变触发器、`known_at >= fetch_time` 和任务组计数不变量均实际拒绝非法写入。
- Migration 可逆性与灾备验收：Downgrade 至 `base` 后 `v3` schema 和版本记录均为空；`pg_dump` 恢复后 9 张表、版本号、测试数据和不可变触发器均正常。
- PostgreSQL 集成测试入口：`tests/v3/test_postgres_integration.py`，由 `V3_TEST_DATABASE_URL` 显式启用；未配置真实数据库时跳过，禁止伪装为已通过。
- 当前全套本地回归：`86 passed, 8 skipped`；3 项 PostgreSQL 集成测试因本机未配置 `V3_TEST_DATABASE_URL` 明确跳过，其等价断言已在服务器隔离 PostgreSQL 17 上通过。
- Repository/Audit Fake 测试覆盖同事务提交、顺序幂等和 Audit 失败回滚；真实 PostgreSQL 进一步通过相同 Task ID 内容冲突及 8 并发幂等验证。
- Phase 2 Domain、Metadata、Migration 与 Legacy 完整回归：`90 passed, 9 skipped`；新增 PostgreSQL 集成项在本机无真实库时明确跳过，等价断言已在服务器 PostgreSQL 17 通过。
- 官方备用 Universe 在服务器实际抓取 5,551 只：上交所 2,315、深交所 2,897、北交所 339，解析数、唯一代码数均等于上游声明数，覆盖率 100%，耗时 5.309 秒；北交所全部为当前 `920xxx` 代码。深交所 XLSX 虽返回 HTTP 200 但只有表头，已拒绝该伪成功并采用受控 JSON 分页。
- Universe fallback 单测覆盖主源失败后 Secondary 接管、覆盖骤降后 LKG、无 LKG 显式失败和主源正常发布；真实 PostgreSQL 17 验证两版快照、最新读取及 `ADDED/CHANGED/REMOVED` 差异各 1 条。
- Universe 单元完成后的全套本地回归：`98 passed, 10 skipped`；其中 5 项 PostgreSQL 集成测试未配置本地数据库而明确跳过，新增 Repository 集成项已在服务器隔离 PostgreSQL 17 通过。
- 日 K 真实探针 20/20 成功：东方财富 0、腾讯接管 14、新浪接管 6；全部取得 300 日 RAW/Factor/FULL Revision，最后 Bar 均为 2026-08-28，平均 1.057 秒，4 并发墙钟 5.872 秒。
- Bar Bundle 在隔离 PostgreSQL 17 通过 nullable 成交额、Factor 外键、首次发布、重复零新增和不可变约束；全套本地回归 `114 passed, 11 skipped`。
- Backfill/Resume V3 专项测试 `43 passed, 7 skipped`；服务器隔离 PostgreSQL 17 端到端验证 Universe 目标读取、2/2 checkpoint、日周月 RAW/QFQ 发布及同 run 重放零新增。
- 严格交易日历对照上交所 2026 春节公告验证 2 月 23 日休市、2 月 24 日开市和节后四交易日短周；服务器 Python 3.12 日历/聚合测试 `6 passed`。
- Backfill 6 目标并发测试确认配置 3 时最大活跃数为 3，两个批次 checkpoint 完整且成功态游标保持常量大小；服务器真实 PostgreSQL 默认并发发布和同 run 重放 `1 passed`。
- 东方财富与官方 Universe 集合实测：主源 5,905、官方 5,551，主源多出的 354 条包含定转、旧代码及退市证券，官方无反向缺口；据此调整为官方 PRIMARY 并新增 5% 异常扩张门禁。
- Phase 2 Job 在全新 PostgreSQL 库从 `0001 -> 0002` 冷启动：官方 5,551 发布成功，首段 20/20；同 run 恢复至 40/40，两个分段均 0 失败；数据库重建 Hash 等于存储 Hash。
- Phase 2 最终全市场任务：`5,551/5,551`、失败 0；发现并修复 UTC `.date()` 导致上海零点退一天的覆盖误判，边界测试与 12.966 秒全市场幂等重放通过。
- 最新 Revision Set：日 RAW/QFQ 各 5,551、周 RAW/QFQ 各 5,551、月 RAW/QFQ 各 5,534；17 只当月新股无完整月，不发布 partial 月 Bar。
- Corporate Action 2025-01-01 起抓取 8,793 个逻辑事件，第三次同事实同步新增 0、unchanged 8,793；现金 8,033、现金加送转 700、送转 60。
- Scheduler `--once` 服务器端到端路径在 23.627 秒完成：Universe 5,551、Backfill 5,551/5,551、400 日公司行动窗口 5,113 条 unchanged，原子 JSON 报告有效；挂载目录 UID 10001 权限门禁已实测。
- 当前全套本地回归：`137 passed, 16 skipped`；新增上海交易日、修订链、公司行动和 advisory lock 真实 PostgreSQL 项均在服务器通过。
- 生产 `market-mcp` 在 Phase 1/2 隔离验收期间持续为 `healthy`；该阶段生产 V3 尚未启用，现状见本文件顶部当前结论。
- P6-01 契约测试已完成；P6-02 在全新 PostgreSQL 16 数据库完整回归为 `217 passed, 5 skipped, 2 warnings`，并完成 `0005 → 0006 → 0005 → 0006` 往返。
- P6-02 服务器 PostgreSQL 17.11 完成 `0005 → 0006 → 0005 → 0006`；专项 `12 passed`，全新数据库完整回归 `212 passed, 5 skipped, 2 warnings`；临时验收资源已清理。
- P6-03 已实现 20–100 候选 Comparison Builder、批量 Feature/Recall/Evidence 读取、确定性 Candidate Set、Hash 幂等短事务发布和 `GET /api/v3/candidates/comparison-pack`；不生成统一 Final Score，不加载分钟 K/深度 Context。
- P6-03 专项回归 `13 passed, 2 skipped, 2 warnings`，全量本地回归 `195 passed, 25 skipped, 2 warnings`；按用户要求未连接服务器。
- P6-04 已实现 FAST/NORMAL/DEEP Context Builder、预算裁剪、反方/冲突优先 Evidence Selection、Untrusted Data 边界及 Context Pack 不可变 Repository/UoW；全量本地回归 `198 passed, 25 skipped, 2 warnings`。
- P6-05 已实现 Task Profile/Expected Run/Task Run Registry、乐观锁、引用校验和确定性 Expected+Task 登记；全量本地回归 `200 passed, 25 skipped, 2 warnings`。
- P6-06 已接通 Context/Evidence/Task/Recall 所需只读 JSON 路由和 Cursor READ；没有新增写接口或 Phase 7 Import；全量本地回归 `202 passed, 25 skipped, 2 warnings`。
- P6-07 使用 5,551 条真实全市场 Feature 和 5,726 条 Recall Result 完成 100 候选/三级 Context 验收；Comparison、NORMAL Context、已存 READ P95 分别为 `127.741ms`、`40.981ms`、`2.527ms`。
- Phase 6 最终修复验收脚本直接入口/连接池参数，以及持久化 Feature/Regime 数值精度导致的错误历史 Hash 重算；读取投影保留源 Hash，发布路径仍严格校验。
- Phase 6 全新 PostgreSQL 17 完整回归 `226 passed, 5 skipped, 2 warnings`，本地完整回归 `206 passed, 25 skipped, 2 warnings`；公网隔离 V3 五类 JSON 和生产 Health/Quote/Kline/Sector/Scan V2 均为 HTTP 200。

## 下一步

1. 进入 RC-03，实现正式全链路 Orchestrator/Scheduler（RC-02D 的 Corporate Action Match 与 Projection Verify Job 随其接入生产调度）；
2. 按整改计划继续 RC-04、RC-05；RC-02 组生产部署验证按阶段边界统一安排；
3. P0/P1 未清零前策略 Release State 保持 `V2`。

当前生产应用镜像基线为 `main@2407c5f`，V3 API、PostgreSQL、Phase 2 Worker、只读行情特征看板、RC-01 安全边界和 RC-02 组整改（Correction Chain、Opening 基线、成本法契约、执行偏差、公司行动 Match、Projection Verify）已启用；策略发布模式仍为 `V2`。

## 接手关键文件

```text
README.md
docs/架构设计实施稿.md
docs/开发规范.md
docs/工作状态.md
docs/README.md
docs/Phase6上下文任务实施记录.md
docs/Phase7-11技术验收报告.md
app/
tests/
```

默认无需阅读 `docs/archive/`。需要追溯 V1/V2 行为或 V3 设计决策时，再根据 `docs/README.md` 定向查看。
