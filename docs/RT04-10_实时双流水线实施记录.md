# RT-04 ～ RT-10 实时双流水线实施记录

> 依据：《gpt-market V3 实时双流水线与AI买卖决策交互_修改方案及详细设计_2026-09-02.md》§27 RT 序列
> 日期：2026-09-02 ～ 2026-09-03
> 方法：TDD（RED→GREEN），每模块测完即推送；fresh-DB 全量回归为提交门槛
> 部署：2026-09-03 已全量部署生产（镜像 `gpt-market:rt10`，迁移 0016）

## 完成清单

| 阶段 | 内容 | 提交 |
| --- | --- | --- |
| RT-04 | AttentionEvent 领域契约（dedupe_key 去抖、append-only、status 流转）、触发引擎、`v3.attention_events` 持久化 + 迁移 0016 | `9073eba` |
| RT-05 | Orchestrator retry（max_attempts）/ fallback / catch-up（补齐错过的交易日）；attempt 号一律由 DB next_attempt 分配 | `7c8e316` |
| RT-06 | EntryPlanPayload 类型化（§17.2）+ Entry Decision Context（`GET /stocks/{code}/decision-context`）：客观 Trigger/Cancel 评估、缺实时数据绝不 READY | `7bdef1e` |
| RT-07 | RecommendedAction 枚举冻结（HOLD/ADD/REDUCE/EXIT，SELL→EXIT 兼容）、REDUCE 必须带 reduce_ratio（§9.6）、Position Decision Context（objective_sell_facts 只陈述绝无建议） | `09069f0` |
| RT-08 | ChatGPT/MCP 只读集成：`/intraday/attention`、`/market/intraday-status`、`/pipeline/eod/latest` 三端点 + 8 个 §19 MCP 工具（PORTFOLIO 组由 `V3_MCP_PORTFOLIO_ENABLED` 门控） | `17c4953` |
| RT-09 | AI Result Import 收口：EntryPlanResult 落库前 EntryPlanPayload 类型化校验、Preview/Confirm/分组提交 AuditEvent 审计链、幂等重确认、全链路不写 trade_ledger | `79f4594` |
| RT-10 | Runtime Closure：BarsOutcomeProvider（点时计算成熟 outcome）、维护链新增 `performance-mature` / `recall-observation-mature` 自动 Job | `02e85fa` |
| 限流韧性 | 东财指数退避加固：可配置指数退避 + 抖动（`EASTMONEY_BACKOFF_BASE`/`EASTMONEY_BACKOFF_CAP`），指数 K 线尝试次数 ≥5（股票行情保持原样快速失败） | 本次 |

## 关键不变量（新增部分）

- AttentionEvent append-only：状态只能 OPEN→ACKED→RESOLVED/EXPIRED；同 dedupe_key 在 cooldown 内去抖
- Entry Readiness：无 plan / 解析失败 / 行情缺失或过期 → NOT_READY（绝不 READY）
- Position Review 只出建议，绝不创建 Trade；REDUCE 无 reduce_ratio 拒绝落库
- AI Result Import 全链路（Decision/EntryPlan/Review/PositionReview/MarketReview/WatchlistProposal）0 行 trade_ledger
- MCP V3 工具全部只读；PORTFOLIO 组默认关闭
- 成熟绩效事实由系统 Job 生成（MaturePerformanceService / BarsOutcomeProvider），输入缺失显式 None，绝不伪造

## 生产部署（2026-09-03）

- 服务器：`root@106.13.171.166`，`/opt/gpt-market`
- 镜像：`gpt-market:rt10`；容器 `market-mcp` + `gpt-market-v3-worker`（docker run 原生重建，参数取自旧容器 inspect；compose v1 不支持 `:-` 插值）
- 迁移：`0015 → 0016_attention_events`（worker 启动自动执行 `alembic upgrade head`）
- 备份：`backups/gpt_market_pre_rt04_20260902.dump`（部署前全量）
- 烟测：4 个新端点 200；MCP v3 工具注册验证通过；维护链三 Job 全绿

### 部署注意（踩坑记录）

1. **代码打进镜像**：容器只挂 `/data`，rsync 后必须 `docker build` + 重建容器，仅 restart 无效
2. **compose 不可用**：服务器 docker-compose 1.17.1 不支持 `${VAR:-}` 插值 → 用 `docker run --env-file`（env 从 `docker inspect` 旧容器提取）
3. **磁盘容量**：镜像约 530MB/个，旧镜像及时清理；`backups/` 每份 ~324MB，保留最近 2 份
4. **ssh 引号**：远程脚本一律先 scp 脚本文件再执行，不内联

## 已知问题：东财指数接口间歇性限流

- 现象：push2his/push2 对该服务器 IP 间歇性连接层丢弃（TLS 成功、请求被静默丢、HTTP 000）；换 UA/Referer 无效；baidu 正常
- 影响：`index-benchmarks` Job 周期性 FAILED → `features` DEPENDENCY_FAILED；catch-up 在下个调度周期自动补齐（2026-09-03 已验证机制生效）
- 收口：指数退避从 0.2s/0.4s 加固为 `2s × 2^n`（上限 60s，±50% 抖动），指数 K 线尝试次数 ≥5
- 进一步选项：配置 `EASTMONEY_PROXY` 走代理出口；或联系机房换出口 IP
- 监控：`select job_id,status,known_at from v3.orchestrator_job_runs where job_id='index-benchmarks' order by known_at desc limit 5;`

## 补全：EOD 主链 Evidence / Full Recall 段（2026-09-03）

> 缺口：设计 §7.2 Step 09-11（Evidence 增量 → Full Recall → Raw Opportunity Publish）
> 一直靠手动脚本（v3_phase4/5）驱动，生产从未自动跑过——Raw Opportunities 初始全 0 的根因之一。
> 本次将三段并入主链 Orchestrator，幂等键 = trade_date，随 RT-05 retry/catch-up 一并生效。

### 变更（`scripts/v3_scheduler.py`）

| Job | 依赖 | 行为 |
| --- | --- | --- |
| `evidence-increment` | features | 24h 窗口 Evidence 增量，复用 `build_registry`（公告/财报/业绩/政策/新闻）；`V3_EVIDENCE_MAX_BATCHES` 默认 1 |
| `full-recall` | evidence-increment | 12 通道 Full Recall；优先取同编排 features Job 的 `feature_run_id`（context.artifacts），追平/重跑退回最新 PUBLISHED run；RawOpp 与 Observation 由 `RunMultiRecallService.publish` 一并落库（Step 11 无独立 Job） |

### 失败策略（生产诚实原则）

- Evidence：部分能力失败不阻断，如实记入 `failed_capabilities`；仅**全部**能力失败才 FAILED（与 index-benchmarks 一致）
- Recall：通道自带可用性声明（`failed_channel_count`/errors）；非 PUBLISHED 才 FAILED
- Recall 的 Feature Run 解析：`_resolve_feature_run_id()` artifacts 优先 → 最新 PUBLISHED run 兜底 → 都没有报错

### 测试

- 主链执行序：`market-data → index-benchmarks → features → evidence-increment → full-recall`
- `_evidence_failed_capabilities`：部分失败上报、全成功空表
- `_resolve_feature_run_id`：artifacts 优先 / latest run 兜底 / 缺失报错
- 回归：`tests/v3` 311 passed, 89 skipped（清除了阻塞收集的遗留未跟踪文件 `test_trade_correction_replay.py`，其配对实现在 stash `trade-correction-replay` 中）

### 生产验证（下次 EOD 或手动 `--once`）

```sql
select job_id, status, attempt, error_summary, known_at
from v3.orchestrator_job_runs
where job_id in ('evidence-increment','full-recall')
order by known_at desc limit 6;
```
