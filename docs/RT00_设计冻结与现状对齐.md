# RT-00 设计冻结与现状对齐

> 依据：《gpt-market V3 实时双流水线与AI买卖决策交互_修改方案及详细设计_2026-09-02.md》（下称《实时方案》）
> 性质：Product Closure Delta，叠加在 V3 Architecture Baseline 1.0 与 RC-00～RC-10 整改成果之上
> 原则：系统=可验证事实+触发事实；AI=综合判断；用户=真实资金确认
> 冲突裁决：与既有实现冲突时，**以《实时方案》为准**（用户 2026-09-02 指定）

## 1. 现状对齐表（已存在 → 不重做）

| 《实时方案》要求 | 现状 | 结论 |
| --- | --- | --- |
| §7.1 EOD 18:45 调度 | `V3_SCHEDULE_AT` 默认 18:45（scripts/v3_scheduler.py），orchestrator 幂等键 + ALREADY_SUCCEEDED 下游续跑（ORC-001 修复） | RT-05 只补 catch-up 显式化与 EOD Job 链对齐 |
| §4.2 provisional 语义 | `MarketBar.provisional` 已存在；发布 Revision 显式拒绝 provisional（market_data.py:441） | 已达标，RT-01 复用 |
| §8.3 EntryReadiness 四态 | `NOT_READY/WAIT_TRIGGER/READY/CANCELLED` 已存在（app/v3/domain/action.py） | 已达标，RT-06 复用 |
| §4.5/RT-02 多周期强制规则 | `_multi_timeframe` 已实现 WEEKLY_DOWN_DAILY_BOUNCE="下降趋势中的反弹"，任一周期 UNKNOWN→UNKNOWN（RC-10 §14 验收通过） | 已达标，RT-02 补 weekly_state 统一与冲突显式化 |
| 60m/15m/5m 结构 | DeepMarketDataService 已有确定性结构 + 单周期故障隔离 + INSUFFICIENT_BARS 诚实 UNKNOWN | 已达标 |
| §12.4 AI Result 回写 | `POST /api/v3/ai-results/imports/preview` + `confirm` 已存在（app/api/v3.py），Preview→Human Confirm 已把守 | RT-09 收口 Schema，不新建通道 |
| §19 权限分离 | MARKET_READ / PORTFOLIO_READ / V3_WRITE / STRATEGY_ADMIN 已实现（app/v3/security.py） | 已达标，新 API 挂既有 scope |
| §9.1 卖出基于真实持仓 | read_position_context 已聚合真实 quantity/cost/多周期/levels/失效条件/evidence/risk/时间效率（RC-10 §14 验收通过） | RT-07 聚合为 decision-context |
| §28.4 known_at 约束 | TEST-001 20 步 E2E + 失败场景测试已把守 | 已达标 |
| 交易日历 | exchange_calendars XSHG 已绑定 | RT-01 交易时段判断直接复用 |
| Legacy Provider | MarketDataService/ProviderManager（东财/腾讯/新浪）、kline_aggregation 已有 | RT-01 复用，不平行造 Provider |

## 2. 净新建缺口（开发工作量集中处）

1. **AttentionEvent / Trigger Engine**（RT-04）：domain + 表 + 评估引擎 + 去抖冷却 + worker —— 完全新建
2. **L0 Quote 快照层 / L1 全市场 Intraday Overlay**（RT-01/RT-03）：worker + 轻量指标 + Active Pool
3. **decision-context 聚合 READ API**（RT-06/RT-07）：Entry/Position 两个聚合接口（只调既有 service）
4. **Typed EntryPlanPayload / PositionReviewPayload**（RT-06/RT-09）：应用层 Pydantic，JSONB 表结构不变
5. **盘中 Evidence 增量调度**（RT-04）：pipeline 已有，缺盘中频率
6. **Intraday Regime**（RT-02/RT-05）
7. Dashboard Live Status / EOD Pipeline 区域

## 3. 设计冲突清单（按《实时方案》裁决）

| # | 冲突点 | 既有实现 | 裁决（以方案为准） |
| --- | --- | --- | --- |
| C-1 | PositionReview `recommended_action` 为自由字符串（默认 HOLD） | models.py String(32) | 应用层强制枚举 HOLD/ADD/REDUCE/EXIT（§9.2/§17.3）；DB 不迁移；导入层兼容 `SELL→EXIT` 一次性映射；旧数据只读原样返回 |
| C-2 | EntryPlanVersion.plan 为自由 dict | JSONB | 表结构不变，应用层新增 EntryPlanPayload 类型化校验（§17.2），仅新数据强制 |
| C-3 | Mode C 受限 MCP Write 下放 | 方案标"可选" | RT-08 仅实现 Mode B（只读 MCP tools）；回写继续走既有 preview/confirm HTTP；Mode C 记 V4 |
| C-4 | 最新 Quote / 分钟 K 存储 | 仅 PostgreSQL + 文件缓存 | 进程内 TTL cache（最小可行，§16.1 Memory 选项）；不引入 Redis；决策用过的分钟事实仍靠 Context Pack 留痕 |
| C-5 | 全市场 Quote/Overlay 频率 | 方案 15–60s / 30–120s 为初始建议 | 配置化默认取保守值（quote 60s、overlay 120s 起），Provider 容量实测后再调（§22 "不是硬编码"）；禁止硬编码 |
| C-6 | 盘中不重复 Full Recall | — | 严格按 §5.4：仅 Overlay Recall + 事件通道 + 存量候选重估 |
| C-7 | 不接券商自动下单 / AI 不写真实 Trade | 已有安全边界 | 维持，新增任何 API 不得提供 confirm_trade 级 AI 写权限（§20） |
| C-9 | 趋势词表 RANGE vs 方案 SIDEWAYS | 特征/结构均用 RANGE | 按 §4.4 统一为 SIDEWAYS（RT-02 已执行）；周线 BASE 为 RC-04-01 冻结细化语义保留（BASE⊂SIDEWAYS 收敛筑底） |
| C-8 | `confirmed_by`/actor_type 必须来自认证 Context | §20.1 | 新接口一律取认证 principal，禁止 Body 自报 |

## 4. 需求追踪更新

《实时方案》§27 RT-01～RT-10 与本文件冲突清单纳入需求追踪矩阵（RT 行新增），
验收标准以《实时方案》§32（32.1–32.6）为最终门禁。

## 5. RT 任务执行约定

- 每个 RT Task：先读方案对应章节 → TDD（RED→GREEN）→ 模块测试全绿 → 中文注释提交推送；
- 不顺手改无关 Phase；发现新冲突记入本文件第 3 节；
- 需要真实验证时提前部署；全部完成后统一部署生产。
