# 行情与 K 线周期能力审计及修复验收（2026-08-29）

## 范围与结论

本轮只修改行情获取、缓存、聚合和故障语义，没有修改 V2 评分公式、权重、主板过滤、候选池或 Top30 排序规则。

服务器 `106.13.171.166` 实际请求显示：东方财富 Quote/K 线节点当前持续断链；腾讯 Quote、前复权日 K 以及 `1m/5m/15m/30m/60m` 均请求成功。修复后，周/月 K 具备稳定的前复权日 K 本地聚合兜底，`15m/30m/60m` 在直接源失败时可由 5 分钟 K 本地聚合。上游和缓存都不可用时仍抛出明确错误，不返回空数组伪装成功。

## Provider × 周期能力矩阵

“声明”表示代码入口接受该周期；“实测”表示 2026-08-29 在部署服务器发出真实请求的结果，不能由 `PERIOD_MAP` 推断。

| 周期 | 东方财富声明 | 东方财富实测 | 腾讯声明 | 腾讯实测 | 本地聚合 | 最终 fallback |
|---|---|---|---|---|---|---|
| Quote | 是 | 失败，连接被上游断开 | 是 | 成功 | 不适用 | 明确 Provider 错误 |
| 独立分时 | 否 | 未实现独立分时协议 | 否 | 未实现独立分时协议 | 不用 1m 冒充分时 | 明确不支持 |
| 1m | 是，`klt=1` | 失败 | 是，`m1` | 成功 | 无 | 旧缓存，否则明确错误 |
| 5m | 是，`klt=5` | 失败 | 是，`m5` | 成功 | 无 | 旧缓存，否则明确错误 |
| 15m | 是，`klt=15` | 失败 | 是，`m15` | 成功 | 5m → 15m | 旧缓存，否则明确错误 |
| 30m | 是，`klt=30` | 失败 | 是，`m30` | 成功 | 5m → 30m | 旧缓存，否则明确错误 |
| 60m | 是，`klt=60` | 失败 | 是，`m60` | 成功 | 5m → 60m | 旧缓存，否则明确错误 |
| day | 是，`klt=101` | 失败 | 是，`day/qfqday/hfqday` | 成功 | 无 | 旧缓存，否则明确错误 |
| week | 是，`klt=102` | 失败 | 否 | 不请求 | qfq day → week | 旧缓存，否则明确错误 |
| month | 是，`klt=103` | 失败 | 否 | 不请求 | qfq day → month | 旧缓存，否则明确错误 |

## 实际接口、参数与复权

| 数据 | 实际接口与参数 | 复权语义 |
|---|---|---|
| 东方财富 Quote | `https://push2*.eastmoney.com/api/qt/stock/get?secid=1.600519&...` | 不适用 |
| 东方财富 K 线 | `https://push2his.eastmoney.com/api/qt/stock/kline/get?secid=1.600519&klt={1,5,15,30,60,101,102,103}&fqt={0,1,2}&lmt=N` | `fqt=1` 为前复权 |
| 腾讯 Quote | `https://qt.gtimg.cn/q=sh600519` | 不适用 |
| 腾讯日 K | `https://proxy.finance.qq.com/ifzqgtimg/appstock/app/fqkline/get?param=sh600519,day,,,N,qfq` | 读取 `qfqday`；raw/hfq 分别读取 `day/hfqday` |
| 腾讯分钟 K | `https://proxy.finance.qq.com/ifzqgtimg/appstock/app/kline/mkline?param=sh600519,m5,,N` | 接口没有可信复权参数；跨交易日时使用同源 raw 日 K 与 qfq/hfq 日 K 的逐日收盘因子统一价格坐标 |

腾讯旧域名 `web.ifzq.gtimg.cn` 在服务器返回 501，现作为代理域名失败后的第二 URL；`proxy.finance.qq.com` 为当前实际成功地址。给 `mkline` 追加 `qfq` 虽返回 HTTP 200，但服务端忽略该参数，因此系统不据此宣称已复权。

## 聚合、partial 与缓存

- 周/月 K 按前复权日 K 的自然周和自然月聚合，OHLC、成交量、成交额分别按开盘首值、最高、最低、收盘末值和求和计算。
- 15/30/60 分钟聚合严格分开 `09:30-11:30` 和 `13:00-15:00`，不会跨午休拼接。
- 当前未结束的日、周、月和分钟 bar 在统一 Service 入口标记 `provisional=true`，Provider 直出和本地聚合遵循同一规则；周末不会把已经收盘的当周误标为 partial。
- L1 内存与 L2 SQLite 现在覆盖全部 K 线周期。分钟缓存键使用完整时间戳，修复了过去同一交易日多根分钟 K 相互覆盖的问题。
- Provider 失败后先尝试对应本地聚合，再尝试足量旧缓存；两者都不可用时抛出 `AllProvidersFailedError`。系统不会静默返回空数据，也不会用其他周期伪装直接周期成功，聚合来源会明确显示为 `aggregate:day:*` 或 `aggregate:5m:*`。

## 派生周期能力

所有收益与位置派生值都来自同一份前复权日 K，不单独请求 Provider。目前真正使用情况如下：

| 派生周期 | 可由日 K 计算 | 当前真实使用方 |
|---|---|---|
| return_3d | 是 | 尚未物化或参与评分 |
| return_5d | 是 | `TechnicalIndicatorService` / 个股技术指标 |
| return_10d | 是 | 尚未物化或参与评分 |
| return_20d | 是 | 技术指标、V2 位置与风险判断 |
| return_60d | 是 | V2 位置与风险判断 |
| return_120d | 是 | V2 位置判断 |
| return_250d | 是 | 尚未物化；V2 使用 250 日价格位置，不使用 250 日收益率 |

## 20 股服务器冷缓存验收

样本覆盖沪深两市及白酒、银行、保险、石油、电力、医药、家电、新能源、电子、券商、建筑、食品、煤炭、面板和钢铁等行业。平均耗时为 20 股并发批次总耗时除以 20，不是单请求串行耗时。

| 周期 | 成功率 | 主源成功 | 腾讯接管 | 本地聚合 | UNKNOWN | 平均耗时 |
|---|---:|---:|---:|---:|---:|---:|
| Quote | 100% | 0 | 20 | 0 | 0 | 79.1 ms |
| 1m | 100% | 0 | 20 | 0 | 0 | 16.0 ms |
| 5m | 100% | 0 | 20 | 0 | 0 | 10.2 ms |
| 15m | 100% | 0 | 20 | 0 | 0 | 10.9 ms |
| 30m | 100% | 0 | 20 | 0 | 0 | 10.5 ms |
| 60m | 100% | 0 | 20 | 0 | 0 | 10.9 ms |
| day | 100% | 0 | 20 | 0 | 0 | 8.9 ms |
| week | 100% | 0 | 0 | 20 | 0 | 78.1 ms |
| month | 100% | 0 | 0 | 20 | 0 | 88.0 ms |

15/30/60 分钟本轮真实请求由腾讯直接成功，因此本地聚合接管数为 0；5m 聚合算法和午休边界由确定性回归测试覆盖。最终测试结果为 `60 passed, 5 skipped`。

## 仍需后续建设

- 独立分时协议尚未实现；1m K 只按 1m K 暴露，不替代分时成交/均价线。
- 交易日判定目前使用周末和时刻规则，法定节假日及调休应接入正式 A 股交易日历。
- 可为 Provider 能力探针增加定时任务，将真实请求成功率持续写入健康端点，而不是只保留发布验收文档。
